# Changelog

บันทึกการเปลี่ยนแปลงที่ผู้ใช้ระบบสังเกตเห็นได้ รูปแบบตาม [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)

หมวดที่ใช้: `Added` `Changed` `Deprecated` `Removed` `Fixed` `Security`

> นี่ไม่ใช่ที่รวม `git log` — ประวัติ commit บันทึกขั้นตอนการเปลี่ยนซอร์สโค้ด ส่วนที่นี่บอกความต่างที่ผู้ใช้ต้องรู้ ซึ่งมักเกิดจากหลาย commit รวมกัน

## [Unreleased]

### Changed

- หัวข้อ ป้ายสถานะ ตัวกรอง และความสูงตารางใช้รูปแบบเดียวกันทุกหน้า หน้าเพิ่มเครื่องอยู่ใต้ทะเบียน และหน้าปีงบแสดงช่วงเดือนพร้อมปีปัจจุบัน (#99)
- หน้าเปรียบเทียบแยกกลุ่มตามสัญญาที่มีผลในเดือนนั้นหรืออาคารจริง ใช้กราฟเดียวสลับตัวชี้วัดได้ และตัดตัวเลือกเดือนกับปุ่มเรียงอันดับที่ซ้ำ (#99)

### Fixed

- ราคาต่อหน้าแสดงทศนิยมได้ครบ 4 ตำแหน่ง รายการค่าใช้จ่ายที่ยังไม่มีราคาไม่แสดงเป็นศูนย์ และหัวคอลัมน์ส่งออกระบุหน้าสุทธิให้ตรงกับข้อมูล (#99)
- หัวตารางที่เริ่มเรียงจากมากไปน้อยสลับทิศได้ สีกราฟคงเดิมเมื่อเปลี่ยนกลุ่มที่เลือก และตัวกรองกับปุ่มตัวชี้วัดไม่ดันหน้าล้นบนมือถือ (#99)

### Added

- Dashboard กดเจาะข้อมูลได้ทุกจุด — กดยอดเงิน เดือนบนกราฟ แผนก หรือสัญญา เพื่อเปิดรายการรายเครื่องที่ค้นหาและส่งออก Excel ได้ตามขอบเขตที่กดเข้าไป พร้อมปุ่มส่งออกการ์ดสรุปเป็นรูปภาพ (#51)
- แดชบอร์ดเลือกเดือนได้หลายเดือนพร้อมปุ่ม "ดูทั้งปีงบ" คลิกจุดบนกราฟเพื่อดูเดือน ยอด และหน่วยของจุดนั้นได้โดยไม่เปลี่ยนตัวกรองของหน้า และแกนตั้งใช้หน่วย "แสน" ตั้งแต่ 100,000 ขึ้นไป (#51)
- กรองตามสัญญาได้ที่แดชบอร์ด ทะเบียนเครื่อง หน้าบันทึกยอดพิมพ์ และรายงานสรุป (#51)
- **เครื่องแต่ละเครื่องมีสถานะการติดตั้งและช่วงเวลาที่ต้องบันทึกยอด แยกจากสถานะใช้งาน/ซ่อม/ปลดระวาง** เพิ่มเครื่องใหม่ต้องเลือกสถานะการติดตั้งก่อนบันทึก และมีหน้า "ตรวจยืนยันการติดตั้ง" (ผู้ดูแลระบบ) สำหรับไล่ตรวจเครื่องเดิม ระบบไม่เดาให้ว่าเครื่องเดิมติดตั้งแล้วหรือยัง ดู [ADR-0018](docs/decisions/0018-separate-installation-status.md) — ฐานข้อมูลที่ใช้งานอยู่ต้องรัน `migration_add_device_service_period.sql` ดู [วิธีรัน migration](docs/how-to/run-migrations.md) (#51, #79)
- นำเข้ายอดพิมพ์จากไอคอนในแถวตัวกรองของหน้าบันทึกยอด (เฉพาะ admin) — ตรวจไฟล์ก่อนแล้วจึงยืนยันบันทึก หน้าตรวจบอกจำนวนรายการใหม่/ที่จะเขียนทับ/ที่ไม่เปลี่ยน ไฟล์ที่มีข้อผิดพลาดจะไม่ถูกบันทึกเลยสักแถวและดาวน์โหลดรายการที่ต้องแก้เป็น CSV ได้ ดู [นำเข้าไฟล์](docs/how-to/import-files.md) (#51)
- แผงรายละเอียดเครื่องและสัญญาบนแดชบอร์ดโหลดทุกแถวตามตัวกรองที่เลือกอยู่ และส่งออกพร้อมปีงบ เดือน และสัญญาที่กำกับยอดนั้น (#51)
- เลือกไทย/อังกฤษจากเมนูบัญชี พร้อมวันที่ ค.ศ. และปีงบ พ.ศ. อ้างอิงในภาษาอังกฤษ โดยคงชื่อข้อมูลและเงิน THB
- ขยายตารางเต็มหน้าจอโดยคงคำค้นและข้อมูลเดิม ค้นหาอันดับรายเครื่องด้วยชื่อรุ่น และแสดงตำแหน่งตามเดือนในรายงานฝ่าย/แผนกและสัญญา
- Excel มีแผ่นบริบทแสดงเวลาออกรายงานและตัวกรอง ดู [วิธีตรวจยอดและติดตามงาน](docs/how-to/use-report-workflow.md)

### Changed

- **หน้ารายงานสรุปและหน้าเปรียบเทียบใช้แถบตัวกรองแถวเดียวเหมือนหน้าอื่นทั้งระบบ** — เดิมสองหน้านี้ใช้การ์ดตัวกรองก้อนใหญ่ (420px และ 290px) ดันตารางและบทสรุปลงไปใต้เส้นพับ ที่ 1440×900 หน้ารายงานเห็นข้อมูลจริงจาก 4 แถวเป็น 12 แถว ตัวกรองที่ใช้ไม่บ่อยย้ายเข้าแผงพับพร้อมป้ายบอกจำนวนที่ใช้อยู่ และหน้ารายงานเหลือช่องค้นหาช่องเดียวจากเดิมสองช่อง (#90)

### Fixed

- **งานราคาที่ค้างพาไปยังจุดที่แก้ได้จริงและไม่ปนยอดคนละปีงบแล้ว** — เครื่องที่ยังไม่ผูกสัญญาจะเปิดฟอร์มของเครื่องนั้นพร้อมวันที่เริ่มคิดเงินที่แนะนำ เครื่องที่ประวัติสัญญาไม่ครอบคลุมจะเปิดฟอร์มพร้อมวันที่เริ่มคิดเงินให้ตรวจ ส่วนหน้าตรวจช่วงสัญญานับเฉพาะปีงบของสัญญา แยกยอดที่อยู่นอกช่วงให้เห็นชัด ปิดปุ่มยืนยันพร้อมบอกเหตุผลเมื่อการยืนยันวันเดิมซ้ำจะไม่เปลี่ยนอะไร และมีลิงก์ไปดูเครื่องของสัญญานั้นเมื่อช่วงถูกต้องแล้วแต่ต้องย้ายเครื่อง การยืนยันช่วงที่สั้นลงไม่ตัดช่วงคิดเงินของเครื่องอีก (เดิมทำให้ยอดของเครื่องที่มีราคาเฉพาะเครื่องหายราคา) ยอดที่อยู่นอกปีงบของสัญญาแยกเป็นงานค้างอีกรายการ (ยังผูกกับสัญญาปีเก่าไม่ได้ ดู #97) การยืนยันใหม่หลังแก้วันสิ้นสุดจะอัปเดตช่วงคิดเงินของเครื่องด้วย และวันที่เริ่มคิดเงินที่ตรวจแล้วมีผลกับยอดย้อนหลังจริง เดิมจึงเกิดอาการสัญญาขึ้นว่า “ยืนยันแล้ว” แต่ยังค้าง 754/500 รายการและกดยืนยันซ้ำไม่หาย (#96)
- ปุ่มและป้ายแจ้งเตือนระดับอันตรายใน dark mode ใช้สีแดงที่สว่างขึ้นกับตัวอักษรเข้ม contrast จึงผ่าน WCAG AA แล้ว (เดิม 4.34:1)

- **รายการในลิ้นชัก "งานที่ต้องติดตาม" กดได้ทั้งแถบแล้ว** — เดิมมีเพียงปุ่มเล็กด้านล่างที่เป็นลิงก์ ทำให้กดชื่อหรือรายละเอียดของเรื่องด่วนแล้วไม่เกิดอะไรขึ้น ตอนนี้รายการพาไปยังหน้าที่แก้ปัญหาได้โดยตรงและใช้คีย์บอร์ดเปิดได้ตามปกติ (#92)

- **หน้าบันทึกยอดพิมพ์เคยขึ้นว่า "กรอกครบแล้ว 0 จาก 46 เครื่อง" ตลอดเวลา** ทั้งที่ทุกช่องในตารางบนหน้าจอเดียวกันมีตัวเลขครบ เกิดจากสองเหตุซ้อนกัน: สรุปยอดทั้งปีไม่เคยถูกโหลดเมื่อปีงบมาถึงหลังเปิดหน้า และเกณฑ์ "กรอกครบ" เทียบกับ 12 เดือนตายตัวซึ่งไม่มีทางครบจนกว่าปีงบจะจบ ตอนนี้เทียบกับเดือนที่ถึงกำหนดแล้ว และใช้จำนวนเครื่องที่ต้องบันทึกยอดจาก API เป็นตัวส่วน (#89)

- **ไฟล์ CSV "รายการที่ต้องแก้" จากหน้านำเข้ายอดพิมพ์ บังคับให้ทุกค่าเป็นข้อความ** — ค่าในไฟล์นั้นมาจากไฟล์ที่ผู้ใช้อัปโหลดและเป็นค่าที่ระบบปฏิเสธไป ซึ่งแปลว่าเป็นค่าที่แปลกที่สุดในไฟล์ต้นทางเสมอ ค่าที่ขึ้นต้นด้วย `=` `+` `-` `@` เคยถูกเขียนลงไปทั้งอย่างนั้นแล้วทำงานเป็นสูตรตอนเปิดใน Excel (#87)

- **นำเข้าทะเบียนเครื่องไม่รับเลขซีเรียลว่าง และไม่ตัดเลขที่ยาวเกิน 100 ตัวอักษรทิ้งเงียบๆ อีกต่อไป** — เดิมทั้งสองกรณีถูกบันทึกลงระบบโดยขึ้นว่า "Import สำเร็จ" ทำให้ได้เครื่องที่ไฟล์ยอดมิเตอร์จับคู่ไม่ได้ตลอดไป ตอนนี้ข้ามเป็นรายแถวพร้อมเหตุผล รวมถึงเลขซีเรียลที่ซ้ำกันเองภายในไฟล์เดียว และชื่อรุ่น/ตำแหน่งที่ยาวเกินขนาดคอลัมน์ (#85)
- ไฟล์ที่เปิดเป็นตารางไม่ได้ เช่นไฟล์ HTML ที่ถูกเปลี่ยนนามสกุลเป็น `.xlsx` ขึ้นข้อความบอกว่าไฟล์เสียแทนที่จะขึ้น "เกิดข้อผิดพลาดในระบบ" (#85)

### Changed

- งานที่ต้องติดตามอยู่ในลิ้นชักแจ้งเตือนที่กดจากกระดิ่งใน Sidebar ที่เดียว ไม่ขึ้นซ้ำบนแดชบอร์ดอีกแล้ว ในลิ้นชักจัดเป็นกลุ่มตามความด่วน (ต้องแก้ทันที / มีงานค้าง / น่าตรวจสอบ) พร้อมจำนวนต่อกลุ่ม และป้ายบนกระดิ่งเปลี่ยนเป็นสีแดงเมื่อมีเรื่องที่ต้องแก้ทันที (#83)

- หัวหน้าที่มีรายการในเมนูไม่แสดงชื่อหมวดซ้ำ ส่วนหน้ารองใช้ชื่อหมวดเดียวกับเมนู และเติมคำแปลอังกฤษที่เคยตกหล่นในข้อความบนหน้าเว็บ (#51)

- **จำนวนหน้าสุทธิหักจากจำนวนหน้าดิบ 2% แทน 20% เดิม และใช้กับยอดย้อนหลังทุกปีงบ** ค่าใช้จ่ายจึงเปลี่ยนแม้จำนวนหน้าดิบที่บันทึกไว้ไม่เปลี่ยน ดู [ADR-0017](docs/decisions/0017-two-percent-page-deduction.md) — ฐานข้อมูลที่ใช้งานอยู่ต้องรัน `migration_update_page_deduction_to_two_percent.sql` ด้วย ไม่งั้นรายงานที่อ่านจาก SQL view จะยังหัก 20% อยู่ ดู [วิธีรัน migration](docs/how-to/run-migrations.md)
- ออกแบบ Dashboard ใหม่: รวมยอดเงินกับกราฟเป็นแผงหลัก วางแผนกและสัญญาให้เห็นพร้อมกัน และใช้หัวหน้า ตัวเลขสรุป และกรอบการ์ดชุดเดียวกับหน้าทะเบียนและค่าใช้จ่าย ไม่มีธีมเฉพาะหน้าใดหน้าหนึ่งอีกแล้ว; งานค้างเปิดจาก Sidebar และกราฟแสดงเฉพาะเดือนที่มีรายการ รวมยอดศูนย์ ดู [แหล่งอ้างอิงงานออกแบบ](docs/explanation/design-references.md) (#51)
- เมนูและหน้า "เปรียบเทียบรายเดือน" เปลี่ยนชื่อเป็น "เปรียบเทียบ" มีสามมุมมองคือตามสัญญา ตามฝ่าย/แผนก และตามอาคาร; กราฟฝ่าย/แผนกย้ายจากหน้าค่าใช้จ่ายมาที่นี่ เลือกฝ่ายแล้วตัวเลือกแผนกจะแคบลงตามฝ่ายนั้น และล้างแผนกทิ้งจะกลับไปเป็นเส้นระดับฝ่าย (#51)
- **ค่าใช้จ่ายของแต่ละเดือนคิดจากราคาที่มีผลในเดือนนั้น ไม่ใช่ราคาปัจจุบัน** สัญญามีช่วงที่มีผลซึ่งผู้ดูแลต้องกดยืนยันจากเอกสารก่อน และมีประวัติว่าเครื่องถูกคิดเงินภายใต้สัญญาไหนในช่วงไหน การแก้ราคาสัญญาหรือย้ายเครื่องไปสัญญาปีงบใหม่จึงไม่เปลี่ยนยอดเงินของเดือนที่ผ่านไปแล้วอีกต่อไป ดู [ADR-0019](docs/decisions/0019-effective-pricing-history.md) — ฐานข้อมูลที่ใช้งานอยู่ต้องรัน `migration_add_effective_pricing.sql` (#51, #81)
- ยอดพิมพ์ที่ยังหาราคาที่มีผลไม่ได้ แสดงว่า "ยังยืนยันราคาไม่ได้" พร้อมจำนวนรายการ ไม่ถูกคิดเป็น 0 บาทอีกต่อไป ยอดรวมทุกหน้าจึงขึ้นชื่อว่า "ค่าใช้จ่ายที่ยืนยันแล้ว" และงดแสดงเปอร์เซ็นต์เพิ่ม-ลดกับอันดับค่าใช้จ่ายจนกว่าราคาจะครบ
- มีหน้า "ยืนยันช่วงที่สัญญามีผล" (ผู้ดูแลระบบ) สำหรับกดรับรองสัญญาทีละฉบับ พร้อมช่องกรอกเอกสารอ้างอิง ระบบเสนอช่วงของปีงบให้เป็นค่าตั้งต้นแต่ไม่ยืนยันแทน
- ปุ่มล้างตัวกรองเด่นขึ้นในทุกหน้าที่มีตัวกรอง (#51)
- กรอบแอปแบบใหม่: แถบเมนูกับแถบบนเป็นพื้นโทน teal ผืนเดียว พื้นที่ทำงานเป็นแผ่นสว่างกว่ามีมุมบนซ้ายโค้ง เมนูที่เลือกเป็นแผ่นขาวชัดเจน และตัวอักษรหลักเข้มขึ้นให้ข้อมูลสำคัญเด่นกว่าข้อมูลรอง ตามทิศทางที่รับรองในผลตรวจรอบที่ 2 (#51)
- ตารางข้อมูล: ปุ่มขยายตารางเป็นไอคอน จำนวนรายการและเลขหน้าย้ายไปแถบล่างของตาราง แถวสลับสีอ่อนให้ไล่ตาตามแถวง่ายขึ้น คอลัมน์แรกปักหมุดเมื่อเลื่อนแนวนอน; แผงแก้ไข/ย้ายเครื่องมีหัวแผงโทนเข้มกว่าเนื้อหาและพื้นหลังที่มืดลงน้อยกว่าเดิม (#51)
- หน้าทะเบียน บันทึกยอด และค่าใช้จ่าย: หัวหน้าเหลือแถวเดียว (จำนวนเครื่อง/ความคืบหน้าปีอยู่ข้างชื่อหน้า) ช่องค้นหา ตัวกรอง และเครื่องมือตารางอยู่แถวเดียวกัน ชื่อฝ่าย/แผนก/ตำแหน่งที่ยาวตัดที่ 2 บรรทัด และยอดรวมของค่าใช้จ่ายเป็นแถบเดียว; ปุ่มเต็มจอขยายเฉพาะตารางข้อมูลยาวพร้อมตรึงจำนวนและเลขหน้าไว้ด้านล่าง ส่วนรายงานทั้งก้อนกับตารางตั้งค่าสั้นๆ ตัดปุ่มออก; ฟอร์มในแผงแก้ไข/ย้ายเครื่องแยกกลุ่มเป็นการ์ด และหัวคอลัมน์ "ยอด ก.ย." มีเว้นวรรค (#51)
- ตัดข้อมูลที่ขึ้นซ้ำ: ชื่อหน้าบนแถบบน (เหลือเฉพาะหน้าย่อย), ป้ายปีงบข้างชื่อหน้าบันทึกยอดและค่าใช้จ่าย (ยังขึ้นให้เห็นตอนสั่งพิมพ์และตอนขยายตารางเต็มจอ ซึ่งแถบบนไม่อยู่ในสายตา), รหัสครุภัณฑ์ใต้ Serial ในทะเบียน, ต้นทางที่ขึ้นสามครั้งในแผงย้าย, คำอธิบายที่ไม่จำเป็นในแผงแก้ไข, หัวรองเหนือชื่อการ์ด; คอลัมน์ราคา/แผ่นซ่อนไว้ก่อน ตัวกรองของตารางอันดับรายเครื่องอยู่ในแผงพับ ข้อความรองในตารางใหญ่และเข้มขึ้น ตัวเลือกช่วงเวลาในหน้าค่าใช้จ่ายเป็นบรรทัดเดียวให้ป้ายตรงแนวกับช่องค้นหา และเมนูใช้ชื่อ "ทะเบียนเครื่องพิมพ์" ตรงกับชื่อหน้า (#51)
- บันทึกยอดเปิดรายเดือนสำหรับ admin/staff พร้อมเดือนค้างจาก API ความคืบหน้าปีกาง/พับได้ และโหมดเต็มจอคงแถบบันทึก; ฟอร์มทั้งปีเตือนก่อนทิ้งค่าที่แก้และมี retry เมื่อโหลดไม่สำเร็จ
- ค่าใช้จ่ายคงคำค้น ส่วนที่กาง และตำแหน่งเลื่อนแยกแท็บเมื่อกลับจากรายละเอียด พร้อมโหลดข้อมูลใหม่; ข้อมูลล้มเหลวไม่แสดงเป็นยอดศูนย์หรือไม่มีรายการ ดู [วิธีตรวจต้นแบบสามหน้า](docs/how-to/review-three-page-prototype.md)
- ทะเบียนค้นหาและกรองจากแถบเดียว แก้ไข/ย้ายผ่าน drawer พร้อมเตือนข้อมูลค้างและคงหน้ารายการหลังบันทึก; ย้าย/ลบอยู่ในเมนูข้อความ และมีปุ่มนำเข้าที่หัวหน้า
- หน้าเข้าสู่ระบบใช้ภาพ SUTH ต้นฉบับกับเลย์เอาต์แบรนด์ 55/45 และแถบเมนูใหม่แบ่งเป็น ภาพรวม/งานประจำ/รายงาน/ตั้งค่าระบบ โดยคง route, สิทธิ์, ค้นหาคำสั่ง, ปีงบ และ preference เดิม
- หน้าล็อกอินยังใช้ลวดลาย teal–ส้มชุดเดียวกับ Dashboard เป็นชั้นประดับที่ไม่รับ pointer โดยพื้นโลโก้และฟอร์มยังเป็นพื้นทึบอ่านง่ายทั้งสองธีม
- เปลี่ยนโลโก้ระบบ (Sidebar, หน้าล็อกอิน, favicon, ไอคอนหน้าจอโฮมของ iOS) เป็น `/brand/suth-horizontal.webp` และ exact-crop จากต้นฉบับเดียวกัน ดู [ที่มาของ brand assets](docs/assets/brand/README.md)
- ปรับหน้าตาทั้งระบบใหม่ผ่าน design system กลาง — ปุ่ม การ์ด ตาราง และหน้าต่างซ้อนหน้าตาเหมือนกันทุกหน้าแล้ว ทั้งโหมดสว่างและมืด ดู [ADR-0008](docs/decisions/0008-design-system-tokens-and-ui-kit.md)
- หน้าข้อมูลอ้างอิง 7 หน้า (ยี่ห้อ, อาคาร, ชั้น, ฝ่าย, แผนก, ปีงบ, ผู้ใช้) ใช้พฤติกรรมร่วมกันแล้ว — ก่อนหน้านี้แต่ละหน้าเป็นไฟล์แยกที่ค่อยๆ เพี้ยนไปคนละทาง บางหน้าตรวจชื่อซ้ำ บางหน้าไม่ตรวจ
- การนำเข้าไฟล์ทะเบียนเครื่องกลายเป็นแท็บในหน้าเพิ่มทรัพย์สิน (`/admin/add-asset?tab=import`) แทนที่จะเป็นหน้าแยก — ลิงก์เดิม `/admin/import-devices` ยัง redirect มาที่นี่ให้อัตโนมัติ
- หน้าที่ดึงข้อมูลอ้างอิงซ้ำกัน (อาคาร แผนก ฝ่าย ยี่ห้อ สัญญา) ไม่ยิงคำขอซ้ำเวลาสลับหน้าไปมาแล้ว ดู [ADR-0009](docs/decisions/0009-tanstack-query-as-the-data-layer.md)
- แดชบอร์ดที่เปิดค้างไว้ (เช่นจอติดผนัง) ดึงตัวเลขใหม่เองทุก 5 นาที ไม่ต้องเดินไปกด F5 — หยุดดึงเมื่อแท็บถูกซ่อน และไม่มีผลกับหน้าบันทึกยอดพิมพ์ ซึ่งตัวเลขต้องไม่ขยับเองระหว่างกรอก
- แต่ละหน้าโหลดเฉพาะโค้ดของตัวเองตอนเปิดใช้จริง ไม่ต้องรอทั้งระบบเหมือนเดิม — กลุ่ม JS ที่ต้องโหลดตอนเปิดเว็บอยู่ที่ ~199 kB (gzip) รวมทุกก้อนที่ `index.html` สั่ง preload ไว้ ไม่ใช่แค่ก้อน entry ก้อนเดียว
- ไลบรารีเขียนไฟล์ Excel (~160 kB gzip) โหลดตอนกดปุ่มส่งออกเท่านั้น เดิมติดมากับทุกหน้าที่มีตารางแม้ไม่ได้กดส่งออกเลย
- ข้อผิดพลาดจาก API ทุกเส้นทางตอบกลับในรูปแบบเดียวกันตามมาตรฐาน Problem Details (RFC 9457) แทนที่รูปแบบเดิมสามแบบที่ปนกันอยู่ — ข้อความ MySQL ดิบ (ชื่อตาราง/คอลัมน์) จะไม่หลุดออกไปถึงเบราว์เซอร์อีกต่อไป ดู [ADR-0010](docs/decisions/0010-problem-details-and-api-conventions.md)
- `GET /api/health` ตรวจการเชื่อมต่อฐานข้อมูลจริงและตอบ `503` เมื่อต่อไม่ได้ แทนที่ `GET /` เดิมที่ตอบ `200` เสมอไม่ว่าฐานข้อมูลจะล่มไปแล้วหรือไม่
- เดือนของยอดพิมพ์เก็บในฐานข้อมูลเป็น ค.ศ. อย่างเดียว และมีการตรวจสอบระดับฐานข้อมูลกันไม่ให้เก็บผิดรูปแบบ ผู้ใช้ยังกรอกและเห็นเป็น พ.ศ. เหมือนเดิม
- **ทุกคนต้องเข้าสู่ระบบใหม่หนึ่งครั้ง** หลังอัปเดตรอบนี้ เพราะเปลี่ยนวิธีเก็บข้อมูลการเข้าสู่ระบบให้ปลอดภัยขึ้น
- ที่อยู่ของเซิร์ฟเวอร์และการตั้งค่าความปลอดภัยย้ายไปอยู่ในไฟล์ตั้งค่า ทำให้ติดตั้งบนเครื่องจริงได้โดยไม่ต้องแก้โค้ด

### Removed

- ถอดเซิร์ฟเวอร์ MCP สำหรับถามข้อมูลผ่านผู้ช่วย AI ออก — ยังไม่เคยเปิดใช้จริงกับผู้ใช้คนใด และคำตอบที่ได้ไม่ตรงกับคำถามในบางกรณีโดยไม่มีอะไรฟ้อง ดู [ADR-0012](docs/decisions/0012-remove-mcp-server.md)
- ไฟล์ตัวอย่างที่ซ้ำซ้อนใน `docs/` — เทมเพลตไฟล์นำเข้าดาวน์โหลดได้จากหน้านำเข้าข้อมูลในระบบแทน

### Fixed

- **กราฟเคารพการตั้งค่า "ลดการเคลื่อนไหว" ของเครื่องแล้ว** เดิมกฎนี้มีผลกับทั้งระบบยกเว้นกราฟ เพราะกราฟเคลื่อนไหวด้วย JavaScript บน canvas ไม่ใช่ CSS
- **หน้าเปรียบเทียบและหน้าฝ่าย/แผนกเคยนับยอดที่ยังยืนยันราคาไม่ได้เป็น 0 บาทเงียบๆ** ทำให้ยอดที่ไม่ครบถูกแสดงเป็นค่าใช้จ่ายจริง ตอนนี้ทั้งสองหน้าบอกจำนวนรายการที่รอราคาไว้ข้างยอดเสมอ เหมือนแดชบอร์ดและหน้าค่าใช้จ่าย
- **หน้าเปรียบเทียบไม่สรุปเปอร์เซ็นต์เพิ่ม–ลดเมื่อข้อมูลยังไม่พอจะสรุป** — เดือนที่ยังยืนยันราคาไม่ครบจะขึ้นยอดของแต่ละเดือนพร้อมเหตุผลแทนเปอร์เซ็นต์ และถ้าสองเดือนที่เทียบกันมีเครื่องบันทึกยอดไม่เท่ากัน จะบอกไว้ด้วยว่ายอดรวมต่างกันได้เองโดยที่การใช้งานไม่เปลี่ยน (#80)
- **ไฟล์ Excel ที่ส่งออกบอกสถานะราคาแล้วทุกไฟล์** ทั้งบรรทัดสรุปในแผ่น "บริบทรายงาน" และคอลัมน์รายแถว เดิมยอดของเครื่องที่ยังยืนยันราคาไม่ได้ถูกเขียนลงไฟล์เป็นเลข 0 ซึ่งคนเปิดไฟล์เอาไปบวกต่อได้โดยไม่มีอะไรบอกว่าไม่ใช่ยอดจริง
- ค่าใช้จ่ายต่อหน้าขึ้นว่า "ยังคำนวณค่าเฉลี่ยครบไม่ได้" แทนตัวเลขที่ต่ำกว่าความจริง เมื่อยังมีรายการที่ยืนยันราคาไม่ได้อยู่ในช่วงที่เลือก
- **ฐานข้อมูลที่ยังไม่ได้รัน migration ทำให้ระบบไม่เปิดพร้อมบอกว่าต้องรันไฟล์ไหน แทนที่จะเปิดขึ้นมาแล้วพังทีละหน้า** เดิมเซิร์ฟเวอร์ตรวจแค่ว่าต่อฐานข้อมูลติดไหม ซึ่งติดเสมอแม้โครงสร้างจะตามโค้ดไม่ทัน ผู้ใช้จึงเป็นคนไปเจอเองในรูปของ "เกิดข้อผิดพลาดในระบบ" และ "โหลดเดือนที่ค้างไม่สำเร็จ" ที่ไม่บอกสาเหตุและไม่บอกทางแก้ ดู [วิธีรัน migration](docs/how-to/run-migrations.md)
- **ยอดเงินรวมของทุกหน้าตรงกันถึงสตางค์สุดท้ายแล้ว** เดิมแดชบอร์ดและรายงานให้ฐานข้อมูลรวมค่าที่ยังไม่ปัดเศษ ส่วนหน้าค่าใช้จ่ายปัดทีละรายการก่อนบวก ยอดรวมของสองทางจึงต่างกันได้ระดับเศษสตางค์เมื่อรวมหลายร้อยรายการ ตอนนี้ปัดเป็นสตางค์ทีละรายการเหมือนกันทุกทาง ตรงกับการคำนวณมือ — ฐานข้อมูลที่ใช้งานอยู่ต้องรัน `migration_round_cost_per_reading.sql` ด้วย
- หน้าบันทึกยอดเปิดเดือนตั้งต้นตรงกับที่ API ตัดสินเสมอ เดิมหน้าเว็บมีกฎสำรองของตัวเองซึ่งให้คนละคำตอบเมื่อไม่มีเดือนค้าง
- **เพิ่มเครื่องใหม่เข้าทะเบียนกลางปีงบ ไม่ทำให้เดือนที่กรอกครบไปแล้วกลายเป็นงานค้างย้อนหลังอีกต่อไป** เดิมความครบถ้วนเทียบกับจำนวนเครื่องที่ใช้งานอยู่ ณ ปัจจุบันเป็นค่าเดียวกันทั้ง 12 เดือน ตอนนี้แต่ละเดือนเทียบกับเครื่องที่ต้องบันทึกยอดของเดือนนั้นจริง (#79)
- ระหว่างที่ยังตรวจยืนยันการติดตั้งไม่ครบ แดชบอร์ดบอกว่า "ยังยืนยันไม่ได้" พร้อมจำนวนเครื่องที่เหลือ แทนการประกาศว่าครบหรือค้าง ซึ่งทั้งสองคำเป็นข้อสรุปที่ข้อมูลยังไม่พอจะพูด (#79)
- ความครบถ้วนบนหน้าบันทึกยอดกับบนแดชบอร์ดเป็นตัวเลขเดียวกันแล้วเมื่อกรองอาคารหรือสัญญา เดิมหน้าบันทึกยอดนับทั้งระบบเสมอไม่ว่าจะกรองอะไรอยู่
- แถบเมนูไม่เปลี่ยนความกว้างเองเวลาเข้าและออกจากแดชบอร์ด (#51)
- คำว่า "ปีงบ" บนปุ่มเลือกปีงบประมาณที่แถบบนอ่านชัดขึ้น เดิมตัวอักษรจางลง 20% จน contrast เหลือ 4.1:1 ต่ำกว่าเกณฑ์ WCAG 1.4.3 (4.5:1) — ตรวจพบตอนเปลี่ยนแถบบนเป็นพื้นทึบ (#51)
- ค่าใช้จ่ายแท็บฝ่าย/แผนกคลิกกางฝ่าย แผนก และเครื่องได้อีกครั้ง เดิมกางได้เฉพาะปุ่ม "กางทั้งหมด" และเปิดยอดรายเดือนหรือลิงก์รายละเอียดของเครื่องจากแท็บนี้ไม่ได้ (#50)
- หน้าค่าใช้จ่ายแจ้งพร้อมปุ่มลองใหม่เมื่อโหลดรายการเครื่องที่ยังไม่ผูกสัญญาหรือรายการเดือนไม่สำเร็จ แทนการซ่อนแถบเตือนเงียบๆ จนดูเหมือนทุกเครื่องถูกนับในยอดแล้ว (#50)
- ช่องเลือกที่มีค่าเดิมเปิดแล้วแสดงตัวเลือกครบ ไม่ใช้ ID ที่เลือกเป็นคำค้นจนชื่ออาคารและรายการอื่นหาย (#48)
- ปุ่ม "ดูประวัติ" ในรายงานสรุปมีพื้นที่กดสูงอย่างน้อย 24px ตามเกณฑ์ 2.5.8 เดิมสูง 15px และเห็นเฉพาะแถวที่มีประวัติย้าย (#48)
- ชื่อแผนกในการ์ดเครื่องที่ใช้งานมากที่สุดขึ้นบรรทัดใหม่แทนการถูกตัดทิ้ง เมื่อผู้ใช้เพิ่มระยะห่างตัวอักษรตามเกณฑ์ 1.4.12 (#48)
- Dashboard แยกความครบถ้วนทั้งปี เช่น 6/12 เดือน ออกจากงานค้างเฉพาะเดือนที่จบแล้ว และเดือนที่ยังไม่ถึงกำหนด
- หน้าเปรียบเทียบรายเดือนเริ่มแสดงทุกเดือนที่มีข้อมูลตามตัวกรอง กราฟรายปีคลิกเลือกเดือนได้โดยยังเห็นบริบททั้งปี
- รายงานย้อนหลังกรองและจัดกลุ่มตามอาคาร ฝ่าย และแผนกที่เครื่องสังกัดในเดือนนั้น พร้อมกันยอดซ้ำเมื่อข้อมูลประวัติมีช่วงซ้อน
- โหมดมืดที่เพี้ยนเป็นหย่อมๆ ในบางหน้า — การ์ดที่ใช้สีเขียว/เหลือง/ม่วงไม่ถูกพลิกตามโหมดเหมือนสีเทา/ฟ้า/แดง ทำให้เป็นแผ่นสีจ้าโผล่กลางหน้าจอโทนมืด
- หัวตารางที่กดเพื่อเรียงลำดับได้ ใช้คีย์บอร์ดกดไม่ได้ (เป็น `<th @click>` ไม่ใช่ปุ่ม) และโปรแกรมอ่านหน้าจอไม่ประกาศว่ากำลังเรียงตามคอลัมน์ไหน
- หน้าต่างซ้อนและช่องเลือกที่เขียนเอง ไม่ขังโฟกัสไว้ข้างใน ไม่คืนโฟกัสตอนปิด และเลื่อนเลือกด้วยลูกศรไม่ได้
- ตัวเลขค่าใช้จ่ายบางรายการแสดงเศษทศนิยมเกินสองตำแหน่ง (เช่น 1209.6000000000001) และยอดรวมคลาดจากการคำนวณมือทีละสตางค์ — ตอนนี้คำนวณเป็นจำนวนเต็มสตางค์ทุกขั้น
- หน้าค่าใช้จ่ายและยอดพิมพ์รายเดือนแสดงข้อมูลว่างเปล่า ทั้งที่มีข้อมูลอยู่ในระบบ สาเหตุคือเดือนในฐานข้อมูลถูกบันทึกเป็น พ.ศ. ขณะที่ระบบกรองด้วย ค.ศ. ตอนนี้ระบบรับเดือนได้ทั้งสองแบบและแปลงให้อัตโนมัติ ดู [ADR-0002](docs/decisions/0002-store-months-in-common-era.md)
- ยอดพิมพ์และค่าใช้จ่ายรายเดือนไม่ตรงกับปีงบที่เลือก เพราะระบบเดาว่าปีงบตรงกับปีปฏิทิน (ม.ค.–ธ.ค.) แทนที่จะเป็นปีงบราชการไทยจริง (ต.ค.–ก.ย.) ดู [ADR-0001](docs/decisions/0001-thai-fiscal-year-oct-sep.md)
- ไฟล์ข้อมูลตัวอย่างสำหรับติดตั้งฐานข้อมูลใหม่รันไม่ผ่านมาตลอด เพราะมีเครื่องหมาย merge conflict ค้างอยู่ในไฟล์

### Security

- ข้อมูลการเข้าสู่ระบบไม่ถูกเก็บไว้ในที่ที่สคริปต์ในหน้าเว็บอ่านได้อีกต่อไป และกดออกจากระบบแล้วหมดผลทันที
- อัปเดตไลบรารีอ่านไฟล์ Excel เป็นรุ่นล่าสุดจากผู้พัฒนาโดยตรง รุ่นบน npm ค้างอยู่ที่ตัวที่มีช่องโหว่และจะไม่ได้รับการแก้ไขอีก ดู [ADR-0003](docs/decisions/0003-sheetjs-from-vendor-registry.md)
- นำรหัสผ่านบัญชีผู้ดูแลระบบออกจากเอกสารและคอมเมนต์ใน schema

## หมายเหตุ

โครงการนี้ยังไม่ได้ออกเวอร์ชันอย่างเป็นทางการ เมื่อเริ่มออกรุ่นให้ใช้ [Semantic Versioning](https://semver.org/lang/th/) และย้ายรายการจาก `Unreleased` ลงหัวข้อเวอร์ชันพร้อมวันที่
