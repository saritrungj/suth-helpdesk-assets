# Changelog

บันทึกการเปลี่ยนแปลงที่ผู้ใช้ระบบสังเกตเห็นได้ รูปแบบตาม [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)

หมวดที่ใช้: `Added` `Changed` `Deprecated` `Removed` `Fixed` `Security`

> นี่ไม่ใช่ที่รวม `git log` — ประวัติ commit บันทึกขั้นตอนการเปลี่ยนซอร์สโค้ด ส่วนที่นี่บอกความต่างที่ผู้ใช้ต้องรู้ ซึ่งมักเกิดจากหลาย commit รวมกัน

## [Unreleased]

### Added

- หน้า **เปรียบเทียบ** แบบรายการหุ้น: ติ๊กรายการในตารางเพื่อวาดลงกราฟ (ค่าเริ่มต้น 3 อันดับแรก สูงสุด 5 เส้น สีช่องติ๊กตรงกับเส้น) พร้อมคอลัมน์สัดส่วนและเส้นแนวโน้มจิ๋วทุกแถว (#212)
- หน้าภาพรวม: เส้นยอดตามใบแจ้งหนี้บนกราฟค่าใช้จ่าย จุดเตือนเดือนที่ยังกรอกไม่ครบ การ์ดเครื่องบอก "จาก N เครื่องที่ใช้งาน" และแถว "ไม่ระบุฝ่าย" มีลิงก์ไปแก้ (#212)
- หน้า **ประวัติการแก้ไข** (ผู้ดูแล): ใครแก้ยอดพิมพ์ เครื่อง สัญญา ปีงบ ข้อมูลอ้างอิง ผู้ใช้ หรือบันทึกงานนำเข้า เมื่อไร จากค่าอะไรเป็นค่าอะไร และการ์ดประวัติในหน้ารายละเอียดเครื่อง — ฐานเดิมต้องรัน `migration_add_audit_log.sql` (#214)
- หน้า **เปรียบเทียบการพิมพ์** (`/compare`) กลับมาในเมนูภาพรวม: ตัวกรองครบทุกมิติ หลายปีงบ และเปรียบเทียบตามฝ่าย แผนก สัญญา อาคาร เครื่อง หรือปีงบ ใช้ตัวเลขชุดเดียวกับหน้าภาพรวม ลิงก์เดิม `/dashboard?by=...` เปิดหน้านี้ให้เอง (#206)
- หน้าภาพรวมเตือนเครื่องที่ยังไม่ระบุฝ่ายหรืออาคาร พร้อมลิงก์ไปทะเบียนที่กรองเฉพาะเครื่องกลุ่มนี้ และนำเข้ารายงานมิเตอร์ที่ไม่มีคอลัมน์ฝ่าย/อาคารจะเตือนตั้งแต่ตอนตรวจไฟล์ (#206)

- ลากขอบขวาของแถบเมนูเพื่อปรับความกว้างได้ (ระบบจำไว้) ลากแคบสุดพับเหลือไอคอน ดับเบิลคลิกคืนค่าเริ่มต้น และกด Ctrl+B พับ/กางได้จากทุกหน้า (#204)
- พื้นหลังไล่สีรุ้งจางๆ ผืนเดียวทั้งแอป รวมแถบเมนูและแถบบน ทั้งโหมดสว่างและมืด (#204)
- ฐานพัฒนาแยกจากฐานจริง (`npm run db:dev:up`) ข้อมูลตัวอย่าง พอร์ต 3308 รหัสคนละชุด — เครื่องพัฒนาเขียนลงข้อมูลจริงโดยไม่ตั้งใจไม่ได้อีก (#194)
- ลบงานนำเข้าที่ยกเลิกหรือหมดอายุและไม่เคยบันทึกทิ้งถาวรได้จากหน้างาน (#192)
- นำเข้าอัตโนมัติ: โยนรายงานของผู้ให้เช่าแล้วระบบสร้างสัญญาจากหัวรายงาน ปีงบ ยี่ห้อ อาคาร ฝ่าย เลือกหมวดของรุ่นที่รู้จัก และบันทึกให้เลย — หยุดถามเฉพาะชื่อที่คล้ายของเดิม รุ่นที่ไม่รู้จัก ยอดที่จะเขียนทับ ตัวเลขสัญญาที่ไม่ตรง หรือเลขซีเรียลที่คล้ายกัน ทุกอย่างที่ระบบทำให้บันทึกในประวัติของงาน ปิดได้ที่ช่องใต้ช่องอัปโหลด (#190)
- หน้ารายละเอียดเครื่องเลือกเปรียบเทียบยอดพิมพ์ได้ 2–3 ปีงบ บนแกนเดือน ต.ค.–ก.ย. เดียวกัน และจดจำปีที่เลือกในลิงก์ (#123)

- หน้า **นำเข้าไฟล์จากผู้ให้เช่า** (เมนูของผู้ดูแล) รวมการนำเข้าทะเบียนเครื่องและยอดมิเตอร์เป็นงานเดียว: อัปโหลดรายงานของผู้ให้เช่าครั้งเดียวได้ทั้งเครื่องและยอด ระบบบอกทีละข้อว่ายังขาดอะไร (สัญญา ปีงบ ชื่อที่ไม่รู้จัก หมวดมิเตอร์ ยอดที่จะเขียนทับ) กรอกสัญญาให้จากไฟล์ ตรวจค่าเช่าและ VAT กับสัญญาเดิม และกระทบยอดกับยอดรวมท้ายรายงานหลังบันทึก (#179, #180)
- งานนำเข้าเก็บไว้ที่เซิร์ฟเวอร์ ปิดหน้าหรือออกจากระบบแล้วกลับมาทำต่อได้ ผู้ดูแลทุกคนเห็นงานของกันและกันพร้อมเจ้าของและเวลาใช้ล่าสุด ทุกงานมีประวัติว่าใครทำอะไรเมื่อไร ดาวน์โหลดไฟล์ต้นฉบับได้ และหมดอายุเมื่อไม่มีใครแตะ 14 วันโดยประวัติยังอยู่ (#179, #180)
- เครื่องและยอดที่มาจากงานนำเข้าบันทึกเลขงานไว้ ตอบได้ว่าข้อมูลแถวนี้มาจากไฟล์ไหน ใครนำเข้า (#179)

- วัดความเร็วที่ผู้ใช้จริงรู้สึก (Core Web Vitals: LCP, INP, CLS) จากเบราว์เซอร์ด้วยไลบรารี web-vitals ของ Google ส่งเข้า log ของ API โดยไม่เก็บตัวตนผู้ใช้ และสรุป p75 รายหน้าพร้อม element ที่เป็นต้นเหตุด้วย `node scripts/web-vitals-report.cjs` ดู [คู่มือ](docs/how-to/measure-web-performance.md) (#171)
- ผู้ดูแลลบสัญญาที่ไม่เคยถูกใช้ได้จากหน้าต่างแก้ไขสัญญา โดยระบบกันสัญญาที่มีเครื่อง ประวัติการคิดเงิน หรืองวดค่าเช่าที่เกิดขึ้นแล้ว (#166)

- หน้านำเข้าทะเบียนเครื่องรับไฟล์ของผู้ให้เช่าได้ตรงๆ (รายงานสถานะเครื่องหลายแผ่น และรายงานมิเตอร์) ตรวจก่อนบันทึก ถามเฉพาะชื่ออาคาร/ฝ่าย/ยี่ห้อที่ไม่รู้จักและหมวดมิเตอร์ของรุ่น แล้วจำคำตอบไว้ใช้ครั้งหน้า เครื่องที่มีอยู่แล้วถูกเติมเฉพาะช่องที่ว่าง และเตือนเลขซีเรียลที่น่าจะพิมพ์ผิด (#132)

- ฐานข้อมูลบน Docker (MySQL 8.4 LTS + phpMyAdmin) เป็นฐานข้อมูลจริงฐานเดียว เปิดด้วย `npm run db:up` พร้อมบัญชีอ่านอย่างเดียวสำหรับดูว่าข้อมูลเข้าหรือยัง และสำรอง/กู้คืนเพื่อย้ายข้อมูลไปเครื่องอื่นด้วย `npm run db:dump` / `db:restore` ดู [คู่มือ](docs/how-to/run-docker-database.md) (#130)
- ชื่อเรียกอื่นของยี่ห้อ อาคาร และฝ่าย ให้ชื่อที่ไฟล์ต่างชุดเขียนต่างกันชี้ไปรายการเดียว — ฐานเดิมต้องรัน `migration_add_master_aliases.sql` (#130)

### Changed

- หน้าภาพรวมไม่แสดงแถบแจ้งเตือนความครบถ้วนของข้อมูลบนหน้า — เรื่องที่ต้องติดตามอยู่ในกระดิ่งแจ้งเตือนที่เดียว
- หน้าตามาตรฐานเดียวทั้งระบบ (#213): มุมโค้งสามขั้น (กล่อง 12px · ปุ่ม/ช่องกรอก 8px · ป้าย 6px) ปุ่มหลักไล่สีแบบแสงเหนือพร้อมแสงเรืองให้เข้ากับพื้นสีรุ้งทั้งสองโหมด ปุ่มรองโปร่งเล็กน้อย ช่องเลือกทุกแบบใช้ลูกศร ▾ เดียวกัน ข้อความรองเข้มขึ้น พื้นสีรุ้งชัดขึ้น และโหมดมืดเป็นน้ำเงินกรมท่าที่การ์ดแยกจากพื้นชัดเจน
- ตัวเลขบนการ์ดสรุปไม่ถูกตัดเป็น "1,588,…" บนมือถืออีก และตารางรายละเอียดของหน้าเปรียบเทียบมีขอบในเหมือนตารางหน้าอื่น (#213)
- ฟอนต์มากับแอป ไม่โหลดจาก Google Fonts — ใช้ได้บนเครือข่ายภายในที่ไม่มีอินเทอร์เน็ต (#213)
- กราฟทุกหน้าเป็นเส้นแบบแอปหุ้น (TradingView Lightweight Charts): พื้นไล่สีใต้เส้น เส้นเล็งพร้อมค่าทุกเส้น ณ เดือนนั้น และป้ายค่าล่าสุด — ไม่มีกราฟแท่งอีก หน้ารายละเอียดเครื่องเทียบปีงบเป็นเส้นซ้อนกัน (#212)
- หน้าเปรียบเทียบซ่อนช่องปีงบและ "เปรียบเทียบตาม ปีงบ" เมื่อมีข้อมูลปีงบเดียว การ์ดตัวเลขไม่แสดง "ยังไม่มีข้อมูลปีก่อนให้เทียบ" อีก และหน้าภาพรวมที่มีไม่เกิน 4 สัญญาเลือกสัญญาด้วยปุ่มได้ทันที (#212)
- นำเข้าไฟล์: ช่องใต้ช่องอัปโหลดเปลี่ยนเป็น **ให้ระบบเตรียมข้อมูลที่ขาดให้ แล้วแสดงก่อนบันทึก** — หน้างานแสดงสรุป "เมื่อกดยืนยัน ระบบจะ…" (สัญญาใหม่พร้อมอายุ ค่าเช่า VAT ราคา ปีงบ ชื่อใหม่ เครื่อง ยอดใหม่ ยอดที่จะแทนที่ ยอดตามใบแจ้งหนี้) แล้วบันทึกเมื่อกด **ยืนยันบันทึก N รายการ** เท่านั้น สัญญาและปีงบจากไฟล์ถูกสร้างพร้อมเครื่องและยอดตอนยืนยัน ยกเลิกงานแล้วไม่มีอะไรค้างในระบบ (#207)
- หน้าภาพรวมเหลือตัวกรองช่วงเวลากับสัญญา ปีงบเปลี่ยนที่แถบบนสุด (บอกไว้บนหน้า) และแสดงกราฟเส้นค่าใช้จ่ายกับยอดพิมพ์รายเดือนคู่กันซ้ายขวา (#206)
- คำบนหน้าจอ ไฟล์ส่งออก และภาษาอังกฤษ: "หน้าที่พิมพ์" เป็น **ยอดพิมพ์** และ "หน้าที่คิดเงิน" เป็น **ยอดพิมพ์หลังหัก 2%** — สูตรหัก 2% ไม่เปลี่ยน (#206)
- กราฟเส้นไม่แกว่งต่ำกว่าจุดจริงอีก และกราฟเส้นเดียวที่ระบายพื้นเริ่มแกนที่ศูนย์ (#206)

### Fixed

- สองคนกรอกยอดเดือนเดียวกันพร้อมกัน คนที่กดทีหลังไม่ทับค่าของอีกคนเงียบๆ แล้ว — ระบบบอกว่ามีคนแก้ไปแล้วและโหลดค่าล่าสุดให้ตรวจก่อน (#214)
- เปิดหน้าที่ไม่มีอยู่หรือหน้าของผู้ดูแลโดยไม่มีสิทธิ์ ระบบบอกเหตุผลก่อนพากลับหน้าภาพรวม (#214)
- การตรวจ "งวดค่าเช่าที่เกิดแล้ว" ก่อนลบสัญญาใช้วันที่ตามเวลาไทย ไม่ใช่เวลา UTC ของฐาน (#214)
- หน้าค่าใช้จ่ายเลือกเดือนที่มีค่าเช่าแต่ไม่มีการพิมพ์ได้ (บอกว่า "ไม่มีข้อมูลพิมพ์") — เดิมเลือกได้เฉพาะเดือนที่มีการพิมพ์ ยอดตามใบแจ้งหนี้ของช่วงที่เลือกได้จึงขาดค่าเช่าของเดือนเหล่านั้น (#208)
- ค้นหาเครื่องบนหน้าค่าใช้จ่ายบอก "พบ 1 จาก 220 เครื่อง" และบอกว่ายอดเงินยังเป็นยอดทั้งสัญญา ปุ่ม Excel บอกว่าส่งออกเฉพาะผลค้นหา และมีบรรทัดส่วนประกอบของยอดตามใบแจ้งหนี้ (ค่าพิมพ์ + ค่าเช่า + VAT) หน้าภาพรวมบอกว่าค่าใช้จ่ายยังไม่รวมค่าเช่าและ VAT (#208)
- เทส web-vitals ผ่านทั้ง `LOG_FORMAT=json` และ `pretty` (#208)
- เครื่องที่ลงทะเบียนจากรายงานมิเตอร์เริ่มนับว่าต้องกรอกที่เดือนสิ้นงวดแรก ไม่ใช่วันต้นงวด — เดิมเดือนก่อนหน้า (เช่น ก.พ.) ค้าง "กรอกครบแล้ว 0 จาก N" ตลอดไปทั้งที่ไม่มีงวดไหนลงเดือนนั้นได้ (#206)
- คำอธิบายกราฟเปรียบเทียบปีงบบอก "แสดง 12 เดือนของปีงบ · มีข้อมูล 6 เดือน" แทน "แสดง 12 เดือนที่มีข้อมูล" และปีงบที่ยังไม่มีข้อมูลบอกไว้ในตัวเลือก (#206)
- กล่องยืนยันลบชั้นหรือแผนกบอกชื่อพร้อมอาคาร/ฝ่าย ไม่ใช่รหัสของอาคาร/ฝ่าย (#206)
- ไอคอนตอนพับแถบเมนู โลโก้ และปุ่มกางเมนูอยู่แนวกลางเดียวกัน (#206)
- ปุ่ม "ล้างทั้งหมด" ในช่องเลือกปีงบของหน้าภาพรวมและหน้ารายละเอียดเครื่องไม่ทำอะไรเลย — ตอนนี้กลับไปปีงบหลัก/คู่ปีเริ่มต้น (#204)
- เมื่อเปลี่ยนปีงบในภาพรวมการพิมพ์ ระบบนำตัวกรองที่ไม่มีข้อมูลในปีใหม่ออกและแจ้งให้ทราบ (#123)

- หน้าบันทึกยอดโหมดภาพรวมและไฟล์ Excel แสดงยอดเดือน ยอดล่าสุด และยอดรวมปีงบจากมิเตอร์ขาวดำตรงกัน พร้อมหัวคอลัมน์ระบุชนิดมิเตอร์ (#159)

- เปิดแอปแล้วได้ปีงบที่ครอบวันนี้เป็นค่าเริ่มต้น เดิมได้ปีสุดท้ายในรายการ เตรียมปีงบล่วงหน้าไว้เมื่อไรทุกหน้าเปิดมาที่ปีในอนาคตซึ่งยังไม่มีข้อมูล ลบปีงบสุดท้ายทิ้งแล้วหน้าต่างๆ ไม่ขอข้อมูลของปีที่ถูกลบอีก (#176)

- หน้าเพิ่มเครื่องไม่ทิ้งข้อมูลที่กรอกค้าง: กดเมนูออกไป สลับไปแท็บนำเข้า หรือรีเฟรช แล้วกลับมาได้ค่าเดิมพร้อมแถบบอกว่ากู้คืนมา กด "ล้างแล้วเริ่มใหม่" เพื่อทิ้งได้ ร่างอยู่เฉพาะในแท็บนั้นและถูกล้างเมื่อออกจากระบบ (#177)

- ลบหรือแก้ไขปีงบประมาณและข้อมูลอ้างอิงแล้ว รายการและตัวเลือกปีงบบนแถบบนแสดงข้อมูลล่าสุดทันที ไม่ค้างจาก HTTP cache ของเบราว์เซอร์ (#136)

- นำเข้ายอดมิเตอร์ปฏิเสธไฟล์ที่ประกาศขอบเขตแผ่นกว้างผิดปกติทันที เดิมไฟล์เล็กที่จัดรูปแบบทั้งแถวทำให้ระบบค้างทุกหน้าหลายวินาทีถึงล่ม — ทั้งสองแบบการนำเข้าใช้เพดานเดียวกัน รวมเพดานเซลล์ใหม่ 1,000,000 เซลล์ (#142)

- นำเข้าทะเบียนจากรายงานมิเตอร์ของผู้ให้เช่าไม่ล้ม (เกิดข้อผิดพลาดในระบบ) เมื่อเลขซีเรียลมีช่องว่างซ้อนหรืออักขระล่องหนจากการคัดลอก และนำเข้ายอดมิเตอร์จับคู่เลขเหล่านั้นกับทะเบียนได้ (#147)

- ราคาพิเศษเฉพาะเครื่องในไฟล์ทะเบียนใช้เกณฑ์เดียวกับฟอร์ม: ทศนิยมเกิน 4 ตำแหน่ง เลขฐานสิบหก หรือราคาสูงผิดปกติ ถูกข้ามพร้อมเหตุผล แทนการถูกปัดเงียบๆ หรือทำให้บันทึกล้ม (#148)

- เพิ่มหรือย้ายเครื่องช่วงเที่ยงคืนถึงเจ็ดโมงเช้าลงวันที่ของประวัติที่ตั้งตามเวลาไทย เดิมได้วันที่เมื่อวาน (UTC) ทำให้การย้ายวันที่ 1 ย้ายยอดทั้งเดือนก่อนไปหน่วยงานใหม่ (#151)

- หน้ารายงานสรุปยอดพิมพ์และไฟล์ Excel นับยอดของเครื่องที่มีมิเตอร์สีครบทุกมิเตอร์ เดิมได้ยอดมิเตอร์เดียวต่อเดือน ยอดรวมจึงต่ำกว่าหน้าอื่นและไม่ตรงกับค่าใช้จ่าย (#146)

- ช่องกรอกยอดรายเดือนของเครื่องที่มีมิเตอร์สีแสดงยอดมิเตอร์ขาวดำเสมอ เดิมอาจแสดงยอดมิเตอร์สีแทน และการล้างช่องนั้นลบยอดขาวดำทิ้ง (#145)

- บันทึกยอดทั้งปีของเครื่องไม่ล้างเลขมิเตอร์ต้นงวด/สิ้นงวดจากไฟล์ผู้ให้เช่าของเดือนที่ไม่ได้แก้อีก เลขมิเตอร์หายเฉพาะเดือนที่จำนวนหน้าเปลี่ยนจริง (#143)

- บันทึกยอดทั้งปีของเครื่องที่มียอดเก่าหาราคาไม่ได้ (ก่อนราคามีผลทันที) บันทึกเดือนอื่นได้ตามปกติ เดิมถูกปฏิเสธทั้งชุดเพราะเดือนเก่าที่ไม่ได้แก้ ยอดที่แก้หรือเพิ่มใหม่ยังต้องหาราคาได้เสมอ (#154)

- นำเข้าทะเบียนเตือนเมื่อไฟล์ระบุสัญญาให้เครื่องที่มีอยู่แล้วแต่ยังไม่ผูกสัญญา เดิมข้ามเงียบๆ เครื่องจึงยังคิดเงินไม่ได้โดยไม่มีใครรู้ (#155)

- นำเข้ายอดมิเตอร์เสร็จแล้ว ตารางกรอกรายเดือนและความครบถ้วนแสดงยอดที่เพิ่งนำเข้าทันที เดิมยังเป็นช่องว่างจนรีเฟรชหน้า ทำให้กรอกทับยอดที่นำเข้าได้ (#144)

- หน้าภาพรวมและหน้าค่าใช้จ่ายไม่ดาวน์โหลดยอดรายมิเตอร์ทุกเดือนทุกปีเพื่อหารายชื่อเดือนอีก (ที่ข้อมูล 5 ปีคือราว 11.6 MB ต่อครั้ง และหน้าภาพรวมดึงซ้ำทุก 5 นาที) (#149)

- บันทึกผลตรวจยืนยันการติดตั้งแล้ว ลิ้นชักงานที่ต้องติดตามและความครบถ้วนอัปเดตทันที เดิมยังนับเครื่องที่เพิ่งตรวจไปอีกหลายนาที (#153)

- แก้ราคาในสัญญาแล้ว รายการสัญญาและราคาแนะนำในฟอร์มเครื่องแสดงราคาใหม่ทันที เดิมยังเป็นราคาเดิมอีกราวหนึ่งนาที (#152)

- เมื่อเว็บกับ API อยู่คนละโดเมน (รวมเครื่องพัฒนา) การดึงข้อมูลอ้างอิงใหม่หลังบันทึกไม่ถูกเบราว์เซอร์บล็อกอีก — เดิม API ไม่อนุญาต header `Cache-Control` ที่เว็บส่ง การล้างแคชหลังแก้ข้อมูลจึงไม่เคยได้ผลข้ามโดเมน (#152)

- ดูผลกระทบก่อนบันทึกสัญญาเทียบยอดตามใบแจ้งหนี้ (ค่าพิมพ์ + ค่าเช่าคงที่ + VAT) แล้ว เดิมเทียบแค่ค่าพิมพ์ เพิ่มหรือแก้ค่าเช่า/VAT จึงขึ้นว่ายอดเงินทุกงวดไม่เปลี่ยน (#156)

- นำเข้ายอดมิเตอร์ที่เจอเลขซีเรียลไม่มีในทะเบียน บอกเลขที่คล้ายกันในทะเบียนด้วย (เช่น `WB…` กับ `BW…`) (#132)

- ชื่อไทยในไฟล์ SQL ของ repo ไม่ถูกเข้ารหัสซ้อนอีกไม่ว่า client ใดโหลด (`SET NAMES utf8mb4;` หัวไฟล์) (#130)

- API ที่ต่อด้วยบัญชีที่ไม่มีสิทธิ์ `SHOW VIEW` บอกเรื่องสิทธิ์แทนการบอกให้รัน migration ซ้ำทั้งที่ฐานครบแล้ว (#130)

### Security

- error ที่ไม่คาดคิดไม่ส่งข้อความภายใน (ชื่อตาราง/คอลัมน์) ไปที่เบราว์เซอร์อีก เว้นเครื่องที่ตั้ง `NODE_ENV=development` — production บนเครื่องนี้ไม่ได้ตั้งค่า จึงเคยส่งออกไปทุกครั้ง (#214)
- ผู้ใช้ที่ถูกลดสิทธิ์ ลบบัญชี หรือเปลี่ยนรหัสผ่าน ใช้ session เดิมทำงานต่อไม่ได้อีก — เดิมสิทธิ์อ่านจาก token ที่อายุ 8 ชั่วโมงอย่างเดียว ทุกคำขอตอนนี้ตรวจบัญชีปัจจุบันในฐานด้วย (#208)
- ไฟล์ .xlsx ที่คลายแล้วใหญ่ผิดปกติถูกปฏิเสธก่อนอ่าน — ไฟล์ 305 KB ที่ข้างในคลายได้ 300 MB เคยทำให้ API ใช้หน่วยความจำ 670 MB และไฟล์ใกล้เพดาน 5 MB ล้ม API ได้ด้วยการอัปโหลดครั้งเดียว (#207)
- กดออกจากระบบแล้ว session ถูกล้างจริง เดิม API ตอบ 500 และ cookie ค้างอยู่ เปิดหน้าใหม่ก็เข้าระบบได้โดยไม่ต้องใส่รหัส — อันตรายกับเครื่องที่ใช้ร่วมกัน คำขอที่ body เป็น JSON ที่อ่านไม่ได้ทุกเส้นทางตอบ 400 แทน 500 (#175)

### Removed

- เส้นทางนำเข้าเดิม `POST /api/devices/import` และ `POST /api/print-transactions/import` และสคริปต์ `scripts/load-vendor-workbooks.cjs` — นำเข้าผ่านงานนำเข้า (`/api/import-sessions`) ทางเดียว ทุกเครื่องและยอดที่นำเข้ามีที่มาและประวัติ (#192)
- คำแปลภาษาอังกฤษ 175 รายการที่ไม่มีหน้าไหนใช้แล้ว พร้อมเทสกันไม่ให้ค้างอีก (#192)
- ไฟล์ Excel ข้อมูลจริง `docs/meter-import-source.xlsx` ออกจาก repo (repo เป็น public) และกันไฟล์ Excel/CSV ทุกไฟล์ใต้ `docs/` (#130)

### Changed

- หน้าภาพรวม: กราฟอยู่ถัดจากการ์ดตัวเลขทันที อันดับฝ่าย/เครื่องย้ายไปใต้กราฟ (#204)
- คำบนหน้าจอเป็นภาษาง่ายขึ้นทั้งระบบ: "ยอดพิมพ์" เป็น "จำนวนพิมพ์" · "ยอดพิมพ์จริง/สุทธิ" เป็น "หน้าที่พิมพ์/หน้าที่คิดเงิน" · "ยอดมิเตอร์" เป็น "เลขมิเตอร์" · "ยอดตามใบแจ้งหนี้" เป็น "เงินตามใบแจ้งหนี้" และตัดคำอธิบายที่ยาวเกินจำเป็น คลังคำอยู่ที่ docs/reference/ui-words.md (#198)
- หน้าภาพรวม: การ์ดตัวเลข 4 ใบเทียบเดือนเดียวกันของปีงบก่อนพร้อมเส้นแนวโน้ม และอันดับฝ่าย/เครื่องที่ใช้มากที่สุด (#197)
- เมนู: ไอคอนมีสีประจำกลุ่ม เมนูที่พับแล้วยังแบ่งกลุ่ม และกระดิ่ง "งานที่ต้องติดตาม" ย้ายไปแถบบน (#196)
- แท็บนำเข้าของหน้าเพิ่มเครื่องและไอคอนนำเข้าในหน้าบันทึกยอดพิมพ์รายเดือนพาไปหน้านำเข้าใหม่ แทนหน้าต่างนำเข้าเดิมสองแบบ และ `/admin/import-devices` เปลี่ยนเส้นทางไป `/admin/import` (#180)
- บันทึกข้อมูลนำเข้าแล้ว หน้าอื่นโหลดข้อมูลใหม่ครั้งเดียว ไม่ใช่หลายครั้งซ้อนกัน (#180)
- หน้าเว็บโหลดเร็วขึ้น: JavaScript ที่ทุกหน้าต้องโหลดก่อนแสดงผล (รวมหน้าเข้าสู่ระบบ) ลดจาก 250.8 เหลือ 186.7 KB (gzip) เพราะไลบรารีกราฟโหลดเฉพาะหน้าที่วาดกราฟ และ `npm run verify` มี performance budget กันไม่ให้โตกลับ (#169)

- แถบเมนูด้านซ้ายเปิดทุกหมวดเป็นค่าเริ่มต้น (ภาพรวม งานประจำ รายงาน ตั้งค่าระบบ) และจำการเปิด-ปิดหมวดที่กดเองไว้ในเบราว์เซอร์ ข้ามการโหลดหน้าและการล็อกอินใหม่ เหมือนการพับแถบเมนู — เดิมเปิดแค่งานประจำและลืมทุกครั้ง (#167)

- สัญญามีอายุของตัวเอง มีราคาต่อหมวดมิเตอร์ ค่าเช่าคงที่ และ VAT; เครื่องรองรับมิเตอร์ขาวดำ/สี และยอดตามใบแจ้งหนี้ปัดระดับรายการราคาต่องวดให้ตรงเอกสารจริง (#128)
- การ์ดหน้าภาพรวมเรียงเป็นยอดพิมพ์จริง → ส่วนลด 2% → ยอดพิมพ์สุทธิ → ค่าใช้จ่าย และหน้านำเข้ามิเตอร์แสดงแผ่นงาน คำเตือน และยอดเทียบใบแจ้งหนี้ก่อนยืนยัน (#128)

- หน้าภาพรวมการพิมพ์เป็นหน้าวิเคราะห์หน้าเดียวของระบบ และมีตัวกรองชั้นเดียว: ปีงบ ช่วงเวลา ฝ่าย แผนก สัญญา อาคาร และเครื่อง เลือกหลายค่าได้ทุกช่อง เลือกแผนกข้ามฝ่ายได้ แล้วตัวเลขสำคัญ กราฟ ตาราง รายละเอียด และไฟล์ที่ส่งออกอ่านจากข้อมูลชุดเดียวกันเสมอ ส่วน “เปรียบเทียบตาม” เปลี่ยนแค่วิธีแบ่ง ไม่เปลี่ยนยอดรวม (#126)

- ปีงบเลือกได้หลายปี (ไม่เกิน 3 ปี) จากตัวกรองบนหน้าเพื่อเทียบข้ามปี โดยเดือนที่เลือกจับคู่ตามตำแหน่งในปีงบ ต.ค.–ก.ย. และปีที่ใหม่ที่สุดที่เลือกคือปีงบของทั้งแอป (#126)

- ปุ่มส่งออกเหลือปุ่มเดียวชื่อ “ส่งออก” แล้วเลือกได้ว่า Excel (รายงานหลายแผ่นพร้อมกราฟ) หรือ CSV (ข้อมูลรายละเอียดแบบแบน) — เดิมมีสามทางออกในสองปุ่มที่เนื้อในซ้ำกันสองอัน (#126)

- ตารางรายละเอียดแสดงครบทุกกลุ่มในขอบเขต ค้นหา เรียง และแบ่งหน้าได้ ส่วนกราฟวาดได้ไม่เกิน 8 กลุ่มตามจำนวนสี แล้วเขียนกำกับว่าแสดงกี่จากกี่รายการ — ไม่เลือกยอดสูงสุดให้แทนผู้ใช้โดยไม่บอกอีกแล้ว (#126)

- Dashboard และหน้าเปรียบเทียบคงยอดและกราฟพร้อมคำอธิบายเดิมแบบจางระหว่างโหลด กราฟเปลี่ยนค่าใน 300 ms และหน้าใหม่จางเข้าใน 150 ms โดยเคารพการลดการเคลื่อนไหว (#115)

- Dashboard และหน้าเปรียบเทียบจำมุมมองและตัวเลือกในแท็บเดิม รองรับการเทียบตามอาคาร เครื่อง และปีงบ (ต.ค.–ก.ย.); การจัดอันดับย้ายไปแผ่น "อันดับ" ใน Excel พร้อมกราฟ 10 อันดับบนและล่าง (#115)

- หัวข้อ ป้ายสถานะ ตัวกรอง และความสูงตารางใช้รูปแบบเดียวกันทุกหน้า หน้าเพิ่มเครื่องอยู่ใต้ทะเบียน และหน้าปีงบแสดงช่วงเดือนพร้อมปีปัจจุบัน (#99)
- หน้าเปรียบเทียบแยกกลุ่มตามสัญญาที่มีผลในเดือนนั้นหรืออาคารจริง ใช้กราฟเดียวสลับตัวชี้วัดได้ และตัดตัวเลือกเดือนกับปุ่มเรียงอันดับที่ซ้ำ (#99)

### Removed

- ถอดขั้นยืนยันราคา หน้า `/admin/contract-prices` และสถานะ “รอราคา”; ราคาในสัญญามีผลทันทีและทางเขียนจะปฏิเสธยอดที่หาราคาไม่ได้ (#128)

- หน้าเปรียบเทียบ (`/compare`) และหน้าค่าใช้จ่ายแยกฝ่าย/แผนก (`/by-department`) ถูกยุบเข้าหน้าภาพรวม ลิงก์และบุ๊กมาร์กเดิมยังเปิดได้โดยแปลงตัวเลือกเป็นตัวกรองของหน้าภาพรวม; การเทียบส่วนต่างแบบ A/B ของหน้าเปรียบเทียบเดิมถูกถอดออกไปพร้อมกัน ยังไม่มีที่แทน ส่วนการกรองตามชั้นใช้การกรองตามอาคารแทน (#126)

### Fixed

- ชื่อไทยในไฟล์ SQL ของ repo ไม่ถูกเข้ารหัสซ้อนอีกไม่ว่า client ใดโหลด (`SET NAMES utf8mb4;` หัวไฟล์) (#130)

- สัญญาที่เริ่มกลางเดือนเริ่มคิดเงินและเก็บค่าเช่าที่งวดแรกจริง (เดือนที่งวดแรกสิ้นสุด) ไม่เก็บค่าเช่าเกินหนึ่งเดือน; แก้เครื่องที่มียอดเก่าหาราคาไม่ได้อยู่แล้วได้ตามปกติ; ไฟล์ผู้ให้เช่าที่ไม่มีราคาแยกมิเตอร์ขาวดำ/สีไม่ได้ถูกปฏิเสธแทนการเดา; สัญญาที่ปีพิมพ์เป็น พ.ศ. ปนหรือยาวเกิน 10 ปีถูกปฏิเสธที่ฟอร์ม (#128)
- สลับปีงบจากแถบบนขณะที่หน้าภาพรวมเลือกหลายปีไว้ ชุดปีที่เทียบและช่วงเดือนของปีเก่าไม่ค้างอยู่อีก — แถบบนกับขอบเขตของตัวเลขบนหน้าหมายถึงปีเดียวกันเสมอ ส่วนตัวกรองหน่วยงาน สัญญา อาคาร และเครื่องยังคงเดิม (#126)
- แตะตัวกรองบนหน้าภาพรวมแล้วจอไม่กระโดดขึ้นบนสุดอีก และไม่มีตัวกรองตัวไหนถูกตัวอื่นเขียนทับหายตอนเลือกหลายตัวติดกัน (#126)
- ตัวกรองทะเบียนเครื่อง ยอดพิมพ์ รายงาน และหน้าเปรียบเทียบใช้ ID แยกชั้นและแผนกชื่อซ้ำ พร้อมชื่ออาคาร/ฝ่ายกำกับ; ลิงก์เก่าแปลงเมื่อระบุได้แน่ชัดและแจ้งเมื่อไม่พบค่า; โหลดตัวเลือกไม่สำเร็จมีปุ่มลองใหม่ (#107)
- นำเข้ายอดพิมพ์: หน้าตรวจไฟล์บอกเดือนที่พบในไฟล์ ดูรายการที่จะเขียนทับได้ครบทุกแถวหรือดาวน์โหลดเป็น CSV ก่อนยืนยัน (เดิมเห็นแค่ 10 แถวแรก) และไฟล์ตัวอย่างใช้เดือนล่าสุดแทน 10/67–12/67 (#106)
- หน้าตั้งค่าข้อมูลอ้างอิง (ชั้น แผนก และหน้าอื่นที่เลือกค่าจากรายการอื่น) บอกเมื่อโหลดรายการอ้างอิงไม่สำเร็จพร้อมปุ่มลองใหม่ ตารางแสดงรหัสแทนขีดที่ดูเหมือนไม่มีข้อมูล และบันทึกไม่ได้จนกว่ารายการจะโหลดสำเร็จ โดยค่าที่กรอกไว้ไม่หาย (#105)
- รายงานสรุปยอดพิมพ์นับยอดของเครื่องที่ย้ายระหว่างปีครั้งเดียวและจัดให้หน่วยงานที่เครื่องอยู่ในเดือนนั้น — กรองฝ่ายเดิมยังเห็นยอดตอนที่อยู่ที่นั่น กรองฝ่ายปัจจุบันไม่พ่วงยอดเก่า และประวัติที่ซ้อนกันไม่ทำให้ยอดเบิ้ล; เดือนที่ไม่มียอดเป็นเซลล์ว่างใน Excel ไม่ใช่ 0; ตัวกรองสถานะเปลี่ยนเป็น "ยอดในเดือนที่แสดง" ซึ่งนับเฉพาะเดือนของแถวนั้น; สลับปีงบเร็วๆ ไม่เห็นยอดปีก่อนค้างอยู่ (#104)
- Dashboard: ตัวเลขสำคัญ แผงรายละเอียด และเปอร์เซ็นต์เทียบช่วงก่อนหน้าใช้รายการที่เลือกชุดเดียวกับกราฟ ตาราง และไฟล์ Excel — เดิมการ์ดเป็นยอดทั้งองค์กรขณะที่กราฟเป็นเฉพาะฝ่ายที่เลือก; การ์ดที่สามนับเครื่องที่มียอดในขอบเขต และมีปุ่ม "วิเคราะห์ส่วนต่าง" พาไปหน้าเปรียบเทียบพร้อมรายการเดิม (#103)
- หน้าเปรียบเทียบโหมดเดือน สัญญา และอาคารส่งออก Excel แบบเดียวกับหน้าภาพรวมได้ และเก็บรายการที่เลือกกับตัวกรองไว้ในลิงก์; ปุ่มส่งออกของตาราง ค่าใช้จ่าย และสรุปตามหน่วยงานกดซ้ำระหว่างสร้างไฟล์ไม่ได้และบอกเมื่อส่งออกไม่สำเร็จพร้อมลองใหม่ (#103)
- หน้าเปรียบเทียบไม่แสดงการ์ดสรุปอัตโนมัติว่างเปล่าอีกแล้ว เมื่อเดือนแรกหรือเดือนสุดท้ายที่เลือกยังไม่มีข้อมูล ระบบจะอธิบายสาเหตุตรง ๆ และยังคงเดือนนั้นเป็นช่องว่างในตาราง; ตัวเลือกประเภทและตัวชี้วัดกลับมาตรงกับ URL เมื่อกดเมนูเดิมหรือใช้ประวัติเบราว์เซอร์ และ query ตัวชี้วัดที่ไม่รู้จักจะกลับไปใช้ค่าเริ่มต้นแทน state ที่เสีย (#101)
- ราคาต่อหน้าแสดงทศนิยมได้ครบ 4 ตำแหน่ง รายการค่าใช้จ่ายที่ยังไม่มีราคาไม่แสดงเป็นศูนย์ และหัวคอลัมน์ส่งออกระบุหน้าสุทธิให้ตรงกับข้อมูล (#99)
- หัวตารางที่เริ่มเรียงจากมากไปน้อยสลับทิศได้ สีกราฟคงเดิมเมื่อเปลี่ยนกลุ่มที่เลือก และตัวกรองกับปุ่มตัวชี้วัดไม่ดันหน้าล้นบนมือถือ (#99)

### Added

- Dashboard เปรียบเทียบภาพรวม ฝ่าย แผนก หรือสัญญาได้จากตัวเลือกชุดเดียว เลือกรายการมาเทียบได้ (อันดับย้ายไปไฟล์ Excel ตาม #115) โดยกำกับว่าอันดับเป็นจุดเริ่มตรวจสอบ ไม่ใช่คำตัดสินความสิ้นเปลือง และหน้าเปรียบเทียบฝ่าย/แผนกแสดงส่วนต่างจากหน่วยงานหรือช่วงฐานที่เลือก; ยอดศูนย์แยกจากข้อมูลที่ยังไม่บันทึก และฐานศูนย์ไม่แสดงเปอร์เซ็นต์ (#103)
- การส่งออกผลเปรียบเทียบรวมเป็นไฟล์ Excel เดียว มีแผ่นกราฟและตารางที่แก้ต่อได้ แผ่นข้อมูลรายละเอียด และแผ่นเงื่อนไขรายงาน พร้อมตัวเลือกข้อมูลดิบอย่างเดียว; กราฟ ตาราง และไฟล์ใช้ขอบเขตเดียวกัน รวมถึงรักษาวันที่ ตัวเลข และเลขศูนย์นำหน้าของรหัส (#103)
- Dashboard กดตัวเลขสำคัญหรือรายการเปรียบเทียบเพื่อเปิดข้อมูลรายเครื่องที่ค้นหาและส่งออก Excel ได้ตามขอบเขตที่เลือก (#51, #103)
- แดชบอร์ดเลือกเดือนได้หลายเดือนพร้อมปุ่ม "ดูทั้งปีงบ" คลิกจุดบนกราฟเพื่อดูเดือน ยอด และหน่วยของจุดนั้นได้โดยไม่เปลี่ยนตัวกรองของหน้า และแกนตั้งใช้หน่วย "แสน" ตั้งแต่ 100,000 ขึ้นไป (#51)
- เลือกสัญญาหลายฉบับเพื่อเปรียบเทียบได้ที่ Dashboard และกรองตามสัญญาได้ที่ทะเบียนเครื่อง หน้าบันทึกยอดพิมพ์ และรายงานสรุป (#51, #103)
- **เครื่องแต่ละเครื่องมีสถานะการติดตั้งและช่วงเวลาที่ต้องบันทึกยอด แยกจากสถานะใช้งาน/ซ่อม/ปลดระวาง** เพิ่มเครื่องใหม่ต้องเลือกสถานะการติดตั้งก่อนบันทึก และมีหน้า "ตรวจยืนยันการติดตั้ง" (ผู้ดูแลระบบ) สำหรับไล่ตรวจเครื่องเดิม ระบบไม่เดาให้ว่าเครื่องเดิมติดตั้งแล้วหรือยัง ดู [ADR-0018](docs/decisions/0018-separate-installation-status.md) — ฐานข้อมูลที่ใช้งานอยู่ต้องรัน `migration_add_device_service_period.sql` ดู [วิธีรัน migration](docs/how-to/run-migrations.md) (#51, #79)
- นำเข้ายอดพิมพ์จากไอคอนในแถวตัวกรองของหน้าบันทึกยอด (เฉพาะ admin) — ตรวจไฟล์ก่อนแล้วจึงยืนยันบันทึก หน้าตรวจบอกจำนวนรายการใหม่/ที่จะเขียนทับ/ที่ไม่เปลี่ยน ไฟล์ที่มีข้อผิดพลาดจะไม่ถูกบันทึกเลยสักแถวและดาวน์โหลดรายการที่ต้องแก้เป็น CSV ได้ ดู [นำเข้าไฟล์](docs/how-to/import-files.md) (#51)
- แผงรายละเอียดเครื่องและสัญญาบนแดชบอร์ดโหลดทุกแถวตามตัวกรองที่เลือกอยู่ และส่งออกพร้อมปีงบ เดือน และสัญญาที่กำกับยอดนั้น (#51)
- เลือกไทย/อังกฤษจากเมนูบัญชี พร้อมวันที่ ค.ศ. และปีงบ พ.ศ. อ้างอิงในภาษาอังกฤษ โดยคงชื่อข้อมูลและเงิน THB
- ขยายตารางเต็มหน้าจอโดยคงคำค้นและข้อมูลเดิม ค้นหาอันดับรายเครื่องด้วยชื่อรุ่น และแสดงตำแหน่งตามเดือนในรายงานฝ่าย/แผนกและสัญญา
- Excel มีแผ่นบริบทแสดงเวลาออกรายงานและตัวกรอง ดู [วิธีตรวจยอดและติดตามงาน](docs/how-to/use-report-workflow.md)

### Changed

- **หน้ารายงานสรุปและหน้าเปรียบเทียบใช้แถบตัวกรองแถวเดียวเหมือนหน้าอื่นทั้งระบบ** — เดิมสองหน้านี้ใช้การ์ดตัวกรองก้อนใหญ่ (420px และ 290px) ดันตารางและบทสรุปลงไปใต้เส้นพับ ที่ 1440×900 หน้ารายงานเห็นข้อมูลจริงจาก 4 แถวเป็น 12 แถว ตัวกรองที่ใช้ไม่บ่อยย้ายเข้าแผงพับพร้อมป้ายบอกจำนวนที่ใช้อยู่ และหน้ารายงานเหลือช่องค้นหาช่องเดียวจากเดิมสองช่อง (#90)

### Fixed

- ชื่อไทยในไฟล์ SQL ของ repo ไม่ถูกเข้ารหัสซ้อนอีกไม่ว่า client ใดโหลด (`SET NAMES utf8mb4;` หัวไฟล์) (#130)

- **งานราคาที่ค้างพาไปยังจุดที่แก้ได้จริงและไม่ปนยอดคนละปีงบแล้ว** — เครื่องที่ยังไม่ผูกสัญญาจะเปิดฟอร์มของเครื่องนั้นพร้อมวันที่เริ่มคิดเงินที่แนะนำ เครื่องที่ประวัติสัญญาไม่ครอบคลุมจะเปิดฟอร์มพร้อมวันที่เริ่มคิดเงินให้ตรวจ ส่วนหน้าตรวจช่วงสัญญานับเฉพาะปีงบของสัญญา แยกยอดที่อยู่นอกช่วงให้เห็นชัด ปิดปุ่มยืนยันพร้อมบอกเหตุผลเมื่อการยืนยันวันเดิมซ้ำจะไม่เปลี่ยนอะไร และมีลิงก์ไปดูเครื่องของสัญญานั้นเมื่อช่วงถูกต้องแล้วแต่ต้องย้ายเครื่อง การยืนยันช่วงที่สั้นลงไม่ตัดช่วงคิดเงินของเครื่องอีก (เดิมทำให้ยอดของเครื่องที่มีราคาเฉพาะเครื่องหายราคา) ยอดที่อยู่นอกปีงบของสัญญาแยกเป็นงานค้างอีกรายการ (ยังผูกกับสัญญาปีเก่าไม่ได้ ดู #97) การยืนยันใหม่หลังแก้วันสิ้นสุดจะอัปเดตช่วงคิดเงินของเครื่องด้วย และวันที่เริ่มคิดเงินที่ตรวจแล้วมีผลกับยอดย้อนหลังจริง เดิมจึงเกิดอาการสัญญาขึ้นว่า “ยืนยันแล้ว” แต่ยังค้าง 754/500 รายการและกดยืนยันซ้ำไม่หาย (#96)
- ปุ่มและป้ายแจ้งเตือนระดับอันตรายใน dark mode ใช้สีแดงที่สว่างขึ้นกับตัวอักษรเข้ม contrast จึงผ่าน WCAG AA แล้ว (เดิม 4.34:1)

- **รายการในลิ้นชัก "งานที่ต้องติดตาม" กดได้ทั้งแถบแล้ว** — เดิมมีเพียงปุ่มเล็กด้านล่างที่เป็นลิงก์ ทำให้กดชื่อหรือรายละเอียดของเรื่องด่วนแล้วไม่เกิดอะไรขึ้น ตอนนี้รายการพาไปยังหน้าที่แก้ปัญหาได้โดยตรงและใช้คีย์บอร์ดเปิดได้ตามปกติ (#92)

- **หน้าบันทึกยอดพิมพ์เคยขึ้นว่า "กรอกครบแล้ว 0 จาก 46 เครื่อง" ตลอดเวลา** ทั้งที่ทุกช่องในตารางบนหน้าจอเดียวกันมีตัวเลขครบ เกิดจากสองเหตุซ้อนกัน: สรุปยอดทั้งปีไม่เคยถูกโหลดเมื่อปีงบมาถึงหลังเปิดหน้า และเกณฑ์ "กรอกครบ" เทียบกับ 12 เดือนตายตัวซึ่งไม่มีทางครบจนกว่าปีงบจะจบ ตอนนี้เทียบกับเดือนที่ถึงกำหนดแล้ว และใช้จำนวนเครื่องที่ต้องบันทึกยอดจาก API เป็นตัวส่วน (#89)

- **ไฟล์ CSV "รายการที่ต้องแก้" จากหน้านำเข้ายอดพิมพ์ บังคับให้ทุกค่าเป็นข้อความ** — ค่าในไฟล์นั้นมาจากไฟล์ที่ผู้ใช้อัปโหลดและเป็นค่าที่ระบบปฏิเสธไป ซึ่งแปลว่าเป็นค่าที่แปลกที่สุดในไฟล์ต้นทางเสมอ ค่าที่ขึ้นต้นด้วย `=` `+` `-` `@` เคยถูกเขียนลงไปทั้งอย่างนั้นแล้วทำงานเป็นสูตรตอนเปิดใน Excel (#87)

- **นำเข้าทะเบียนเครื่องไม่รับเลขซีเรียลว่าง และไม่ตัดเลขที่ยาวเกิน 100 ตัวอักษรทิ้งเงียบๆ อีกต่อไป** — เดิมทั้งสองกรณีถูกบันทึกลงระบบโดยขึ้นว่า "Import สำเร็จ" ทำให้ได้เครื่องที่ไฟล์ยอดมิเตอร์จับคู่ไม่ได้ตลอดไป ตอนนี้ข้ามเป็นรายแถวพร้อมเหตุผล รวมถึงเลขซีเรียลที่ซ้ำกันเองภายในไฟล์เดียว และชื่อรุ่น/ตำแหน่งที่ยาวเกินขนาดคอลัมน์ (#85)
- ไฟล์ที่เปิดเป็นตารางไม่ได้ เช่นไฟล์ HTML ที่ถูกเปลี่ยนนามสกุลเป็น `.xlsx` ขึ้นข้อความบอกว่าไฟล์เสียแทนที่จะขึ้น "เกิดข้อผิดพลาดในระบบ" (#85)

### Changed

- งานที่ต้องติดตามอยู่ในลิ้นชักแจ้งเตือนที่กดจากกระดิ่งใน Sidebar ที่เดียว ไม่ขึ้นซ้ำบนแดชบอร์ดอีกแล้ว ในลิ้นชักจัดเป็นกลุ่มตามความด่วน (ต้องแก้ทันที / มีงานค้าง / น่าตรวจสอบ) พร้อมจำนวนต่อกลุ่ม และป้ายบนกระดิ่งเปลี่ยนเป็นสีแดงเมื่อมีเรื่องที่ต้องแก้ทันที (#83)

- หัวหน้าที่มีรายการในเมนูไม่แสดงชื่อหมวดซ้ำ ส่วนหน้ารองใช้ชื่อหมวดเดียวกับเมนู และเติมคำแปลอังกฤษที่เคยตกหล่นในข้อความบนหน้าเว็บ (#51)

- **จำนวนหน้าสุทธิหักจากจำนวนหน้าดิบ 2% แทน 20% เดิม และใช้กับยอดย้อนหลังทุกปีงบ** ค่าใช้จ่ายจึงเปลี่ยนแม้จำนวนหน้าดิบที่บันทึกไว้ไม่เปลี่ยน ดู [ADR-0017](docs/decisions/0017-two-percent-page-deduction.md) — ฐานข้อมูลที่ใช้งานอยู่ต้องรัน `migration_update_page_deduction_to_two_percent.sql` ด้วย ไม่งั้นรายงานที่อ่านจาก SQL view จะยังหัก 20% อยู่ ดู [วิธีรัน migration](docs/how-to/run-migrations.md)
- ออกแบบ Dashboard ใหม่: รวมยอดเงินกับกราฟเป็นแผงหลัก วางแผนกและสัญญาให้เห็นพร้อมกัน และใช้หัวหน้า ตัวเลขสรุป และกรอบการ์ดชุดเดียวกับหน้าทะเบียนและค่าใช้จ่าย ไม่มีธีมเฉพาะหน้าใดหน้าหนึ่งอีกแล้ว; งานค้างเปิดจาก Sidebar และกราฟแสดงเฉพาะเดือนที่มีรายการ รวมยอดศูนย์ ดู [แหล่งอ้างอิงงานออกแบบ](docs/explanation/design-references.md) (#51)
- เมนูและหน้า "เปรียบเทียบรายเดือน" เปลี่ยนชื่อเป็น "เปรียบเทียบ" มีสามมุมมองคือตามสัญญา ตามฝ่าย/แผนก และตามอาคาร; กราฟฝ่าย/แผนกย้ายจากหน้าค่าใช้จ่ายมาที่นี่ เลือกฝ่ายแล้วตัวเลือกแผนกจะแคบลงตามฝ่ายนั้น และล้างแผนกทิ้งจะกลับไปเป็นเส้นระดับฝ่าย (#51)
- **ค่าใช้จ่ายของแต่ละเดือนคิดจากราคาที่มีผลในเดือนนั้น ไม่ใช่ราคาปัจจุบัน** สัญญามีช่วงที่มีผลซึ่งผู้ดูแลต้องกดยืนยันจากเอกสารก่อน และมีประวัติว่าเครื่องถูกคิดเงินภายใต้สัญญาไหนในช่วงไหน การแก้ราคาสัญญาหรือย้ายเครื่องไปสัญญาปีงบใหม่จึงไม่เปลี่ยนยอดเงินของเดือนที่ผ่านไปแล้วอีกต่อไป ดู [ADR-0019](docs/decisions/0019-effective-pricing-history.md) — ฐานข้อมูลที่ใช้งานอยู่ต้องรัน `migration_add_effective_pricing.sql` (#51, #81)
- ยอดพิมพ์ที่ยังหาราคาที่มีผลไม่ได้ แสดงว่า "ยังยืนยันราคาไม่ได้" พร้อมจำนวนรายการ ไม่ถูกคิดเป็น 0 บาทอีกต่อไป ยอดรวมทุกหน้าจึงขึ้นชื่อว่า "ค่าใช้จ่ายที่ยืนยันแล้ว" และงดแสดงเปอร์เซ็นต์เพิ่ม-ลดกับอันดับค่าใช้จ่ายจนกว่าราคาจะครบ
- มีหน้า "ยืนยันช่วงที่สัญญามีผล" (ผู้ดูแลระบบ) สำหรับกดรับรองสัญญาทีละฉบับ พร้อมช่องกรอกเอกสารอ้างอิง ระบบเสนอช่วงของปีงบให้เป็นค่าตั้งต้นแต่ไม่ยืนยันแทน
- ปุ่มล้างตัวกรองเด่นขึ้นในทุกหน้าที่มีตัวกรอง (#51)
- กรอบแอปแบบใหม่: แถบเมนูกับแถบบนเป็นพื้นโทน teal ผืนเดียว พื้นที่ทำงานเป็นแผ่นสว่างกว่ามีมุมบนซ้ายโค้ง เมนูที่เลือกเป็นแผ่นขาวชัดเจน และตัวอักษรหลักเข้มขึ้นให้ข้อมูลสำคัญเด่นกว่าข้อมูลรอง ตามทิศทางที่รับรองในผลตรวจรอบที่ 2 (#51)
- ตารางข้อมูล: ปุ่มขยายตารางเป็นไอคอน จำนวนรายการและเลขหน้าย้ายไปแถบล่างของตาราง แถวสลับสีอ่อนให้ไล่ตาตามแถวง่ายขึ้น คอลัมน์แรกปักหมุดเมื่อเลื่อนแนวนอน; แผงแก้ไข/ย้ายเครื่องมีหัวแผงโทนเข้มกว่าเนื้อหาและพื้นหลังที่มืดลงน้อยกว่าเดิม (#51)
- หน้าทะเบียน บันทึกยอด และค่าใช้จ่าย: หัวหน้าเหลือแถวเดียว (จำนวนเครื่อง/ความคืบหน้าปีอยู่ข้างชื่อหน้า) ช่องค้นหา ตัวกรอง และเครื่องมือตารางอยู่แถวเดียวกัน ชื่อฝ่าย/แผนก/ตำแหน่งที่ยาวตัดที่ 2 บรรทัด และยอดรวมของค่าใช้จ่ายเป็นแถบเดียว; ปุ่มเต็มจอขยายเฉพาะตารางข้อมูลยาวพร้อมตรึงจำนวนและเลขหน้าไว้ด้านล่าง ส่วนรายงานทั้งก้อนกับตารางตั้งค่าสั้นๆ ตัดปุ่มออก; ฟอร์มในแผงแก้ไข/ย้ายเครื่องแยกกลุ่มเป็นการ์ด และหัวคอลัมน์ "ยอด ก.ย." มีเว้นวรรค (#51)
- ตัดข้อมูลที่ขึ้นซ้ำ: ชื่อหน้าบนแถบบน (เหลือเฉพาะหน้าย่อย), ป้ายปีงบข้างชื่อหน้าบันทึกยอดและค่าใช้จ่าย (ยังขึ้นให้เห็นตอนสั่งพิมพ์และตอนขยายตารางเต็มจอ ซึ่งแถบบนไม่อยู่ในสายตา), รหัสครุภัณฑ์ใต้ Serial ในทะเบียน, ต้นทางที่ขึ้นสามครั้งในแผงย้าย, คำอธิบายที่ไม่จำเป็นในแผงแก้ไข, หัวรองเหนือชื่อการ์ด; คอลัมน์ราคา/แผ่นซ่อนไว้ก่อน ตัวกรองของตารางอันดับรายเครื่องอยู่ในแผงพับ ข้อความรองในตารางใหญ่และเข้มขึ้น ตัวเลือกช่วงเวลาในหน้าค่าใช้จ่ายเป็นบรรทัดเดียวให้ป้ายตรงแนวกับช่องค้นหา และเมนูใช้ชื่อ "ทะเบียนเครื่องพิมพ์" ตรงกับชื่อหน้า (#51)
- บันทึกยอดเปิดรายเดือนสำหรับ admin/staff พร้อมเดือนค้างจาก API ความคืบหน้าปีกาง/พับได้ และโหมดเต็มจอคงแถบบันทึก; ฟอร์มทั้งปีเตือนก่อนทิ้งค่าที่แก้และมี retry เมื่อโหลดไม่สำเร็จ
- ค่าใช้จ่ายคงคำค้น ส่วนที่กาง และตำแหน่งเลื่อนแยกแท็บเมื่อกลับจากรายละเอียด พร้อมโหลดข้อมูลใหม่; ข้อมูลล้มเหลวไม่แสดงเป็นยอดศูนย์หรือไม่มีรายการ ดู [วิธีตรวจต้นแบบสามหน้า](docs/how-to/review-three-page-prototype.md)
- ทะเบียนค้นหาและกรองจากแถบเดียว แก้ไข/ย้ายผ่าน drawer พร้อมเตือนข้อมูลค้างและคงหน้ารายการหลังบันทึก; ย้าย/ลบอยู่ในเมนูข้อความ และมีปุ่มนำเข้าที่หัวหน้า
- หน้าเข้าสู่ระบบใช้ภาพ SUTH ต้นฉบับกับเลย์เอาต์แบรนด์ 55/45 และแถบเมนูใหม่แบ่งเป็น ภาพรวม/งานประจำ/รายงาน/ตั้งค่าระบบ โดยคง route, สิทธิ์, ค้นหาคำสั่ง, ปีงบ และ preference เดิม
- หน้าล็อกอินยังใช้ลวดลาย teal–ส้มชุดเดียวกับ Dashboard เป็นชั้นประดับที่ไม่รับ pointer โดยพื้นโลโก้และฟอร์มยังเป็นพื้นทึบอ่านง่ายทั้งสองธีม
- เปลี่ยนโลโก้ระบบ (Sidebar, หน้าล็อกอิน, favicon, ไอคอนหน้าจอโฮมของ iOS) เป็น `/brand/suth-horizontal.webp` และ exact-crop จากต้นฉบับเดียวกัน ดู [ที่มาของ brand assets](docs/assets/brand/README.md)
- ปรับหน้าตาทั้งระบบใหม่ผ่าน design system กลาง — ปุ่ม การ์ด ตาราง และหน้าต่างซ้อนหน้าตาเหมือนกันทุกหน้าแล้ว ทั้งโหมดสว่างและมืด ดู [ADR-0008](docs/decisions/0008-design-system-tokens-and-ui-kit.md)
- หน้าข้อมูลอ้างอิง 7 หน้า (ยี่ห้อ, อาคาร, ชั้น, ฝ่าย, แผนก, ปีงบ, ผู้ใช้) ใช้พฤติกรรมร่วมกันแล้ว — ก่อนหน้านี้แต่ละหน้าเป็นไฟล์แยกที่ค่อยๆ เพี้ยนไปคนละทาง บางหน้าตรวจชื่อซ้ำ บางหน้าไม่ตรวจ
- การนำเข้าไฟล์ทะเบียนเครื่องกลายเป็นแท็บในหน้าเพิ่มทรัพย์สิน (`/admin/add-asset?tab=import`) แทนที่จะเป็นหน้าแยก — ลิงก์เดิม `/admin/import-devices` ยัง redirect มาที่นี่ให้อัตโนมัติ
- หน้าที่ดึงข้อมูลอ้างอิงซ้ำกัน (อาคาร แผนก ฝ่าย ยี่ห้อ สัญญา) ไม่ยิงคำขอซ้ำเวลาสลับหน้าไปมาแล้ว ดู [ADR-0009](docs/decisions/0009-tanstack-query-as-the-data-layer.md)
- แดชบอร์ดที่เปิดค้างไว้ (เช่นจอติดผนัง) ดึงตัวเลขใหม่เองทุก 5 นาที ไม่ต้องเดินไปกด F5 — หยุดดึงเมื่อแท็บถูกซ่อน และไม่มีผลกับหน้าบันทึกยอดพิมพ์ ซึ่งตัวเลขต้องไม่ขยับเองระหว่างกรอก
- แต่ละหน้าโหลดเฉพาะโค้ดของตัวเองตอนเปิดใช้จริง ไม่ต้องรอทั้งระบบเหมือนเดิม — กลุ่ม JS ที่ต้องโหลดตอนเปิดเว็บอยู่ที่ ~199 kB (gzip) รวมทุกก้อนที่ `index.html` สั่ง preload ไว้ ไม่ใช่แค่ก้อน entry ก้อนเดียว
- ไลบรารีเขียนไฟล์ Excel (~160 kB gzip) โหลดตอนกดปุ่มส่งออกเท่านั้น เดิมติดมากับทุกหน้าที่มีตารางแม้ไม่ได้กดส่งออกเลย
- ข้อผิดพลาดจาก API ทุกเส้นทางตอบกลับในรูปแบบเดียวกันตามมาตรฐาน Problem Details (RFC 9457) แทนที่รูปแบบเดิมสามแบบที่ปนกันอยู่ — ข้อความ MySQL ดิบ (ชื่อตาราง/คอลัมน์) จะไม่หลุดออกไปถึงเบราว์เซอร์อีกต่อไป ดู [ADR-0010](docs/decisions/0010-problem-details-and-api-conventions.md)
- `GET /api/health` ตรวจการเชื่อมต่อฐานข้อมูลจริงและตอบ `503` เมื่อต่อไม่ได้ แทนที่ `GET /` เดิมที่ตอบ `200` เสมอไม่ว่าฐานข้อมูลจะล่มไปแล้วหรือไม่
- เดือนของยอดพิมพ์เก็บในฐานข้อมูลเป็น ค.ศ. อย่างเดียว และมีการตรวจสอบระดับฐานข้อมูลกันไม่ให้เก็บผิดรูปแบบ ผู้ใช้ยังกรอกและเห็นเป็น พ.ศ. เหมือนเดิม
- **ทุกคนต้องเข้าสู่ระบบใหม่หนึ่งครั้ง** หลังอัปเดตรอบนี้ เพราะเปลี่ยนวิธีเก็บข้อมูลการเข้าสู่ระบบให้ปลอดภัยขึ้น
- ที่อยู่ของเซิร์ฟเวอร์และการตั้งค่าความปลอดภัยย้ายไปอยู่ในไฟล์ตั้งค่า ทำให้ติดตั้งบนเครื่องจริงได้โดยไม่ต้องแก้โค้ด

### Removed

- ถอดเซิร์ฟเวอร์ MCP สำหรับถามข้อมูลผ่านผู้ช่วย AI ออก — ยังไม่เคยเปิดใช้จริงกับผู้ใช้คนใด และคำตอบที่ได้ไม่ตรงกับคำถามในบางกรณีโดยไม่มีอะไรฟ้อง ดู [ADR-0012](docs/decisions/0012-remove-mcp-server.md)
- ไฟล์ตัวอย่างที่ซ้ำซ้อนใน `docs/` — เทมเพลตไฟล์นำเข้าดาวน์โหลดได้จากหน้านำเข้าข้อมูลในระบบแทน

### Fixed

- ชื่อไทยในไฟล์ SQL ของ repo ไม่ถูกเข้ารหัสซ้อนอีกไม่ว่า client ใดโหลด (`SET NAMES utf8mb4;` หัวไฟล์) (#130)

- **กราฟเคารพการตั้งค่า "ลดการเคลื่อนไหว" ของเครื่องแล้ว** เดิมกฎนี้มีผลกับทั้งระบบยกเว้นกราฟ เพราะกราฟเคลื่อนไหวด้วย JavaScript บน canvas ไม่ใช่ CSS
- **หน้าเปรียบเทียบและหน้าฝ่าย/แผนกเคยนับยอดที่ยังยืนยันราคาไม่ได้เป็น 0 บาทเงียบๆ** ทำให้ยอดที่ไม่ครบถูกแสดงเป็นค่าใช้จ่ายจริง ตอนนี้ทั้งสองหน้าบอกจำนวนรายการที่รอราคาไว้ข้างยอดเสมอ เหมือนแดชบอร์ดและหน้าค่าใช้จ่าย
- **หน้าเปรียบเทียบไม่สรุปเปอร์เซ็นต์เพิ่ม–ลดเมื่อข้อมูลยังไม่พอจะสรุป** — เดือนที่ยังยืนยันราคาไม่ครบจะขึ้นยอดของแต่ละเดือนพร้อมเหตุผลแทนเปอร์เซ็นต์ และถ้าสองเดือนที่เทียบกันมีเครื่องบันทึกยอดไม่เท่ากัน จะบอกไว้ด้วยว่ายอดรวมต่างกันได้เองโดยที่การใช้งานไม่เปลี่ยน (#80)
- **ไฟล์ Excel ที่ส่งออกบอกสถานะราคาแล้วทุกไฟล์** ทั้งบรรทัดสรุปในแผ่น "บริบทรายงาน" และคอลัมน์รายแถว เดิมยอดของเครื่องที่ยังยืนยันราคาไม่ได้ถูกเขียนลงไฟล์เป็นเลข 0 ซึ่งคนเปิดไฟล์เอาไปบวกต่อได้โดยไม่มีอะไรบอกว่าไม่ใช่ยอดจริง
- ค่าใช้จ่ายต่อหน้าขึ้นว่า "ยังคำนวณค่าเฉลี่ยครบไม่ได้" แทนตัวเลขที่ต่ำกว่าความจริง เมื่อยังมีรายการที่ยืนยันราคาไม่ได้อยู่ในช่วงที่เลือก
- **ฐานข้อมูลที่ยังไม่ได้รัน migration ทำให้ระบบไม่เปิดพร้อมบอกว่าต้องรันไฟล์ไหน แทนที่จะเปิดขึ้นมาแล้วพังทีละหน้า** เดิมเซิร์ฟเวอร์ตรวจแค่ว่าต่อฐานข้อมูลติดไหม ซึ่งติดเสมอแม้โครงสร้างจะตามโค้ดไม่ทัน ผู้ใช้จึงเป็นคนไปเจอเองในรูปของ "เกิดข้อผิดพลาดในระบบ" และ "โหลดเดือนที่ค้างไม่สำเร็จ" ที่ไม่บอกสาเหตุและไม่บอกทางแก้ ดู [วิธีรัน migration](docs/how-to/run-migrations.md)
- **ยอดเงินรวมของทุกหน้าตรงกันถึงสตางค์สุดท้ายแล้ว** เดิมแดชบอร์ดและรายงานให้ฐานข้อมูลรวมค่าที่ยังไม่ปัดเศษ ส่วนหน้าค่าใช้จ่ายปัดทีละรายการก่อนบวก ยอดรวมของสองทางจึงต่างกันได้ระดับเศษสตางค์เมื่อรวมหลายร้อยรายการ ตอนนี้ปัดเป็นสตางค์ทีละรายการเหมือนกันทุกทาง ตรงกับการคำนวณมือ — ฐานข้อมูลที่ใช้งานอยู่ต้องรัน `migration_round_cost_per_reading.sql` ด้วย
- หน้าบันทึกยอดเปิดเดือนตั้งต้นตรงกับที่ API ตัดสินเสมอ เดิมหน้าเว็บมีกฎสำรองของตัวเองซึ่งให้คนละคำตอบเมื่อไม่มีเดือนค้าง
- **เพิ่มเครื่องใหม่เข้าทะเบียนกลางปีงบ ไม่ทำให้เดือนที่กรอกครบไปแล้วกลายเป็นงานค้างย้อนหลังอีกต่อไป** เดิมความครบถ้วนเทียบกับจำนวนเครื่องที่ใช้งานอยู่ ณ ปัจจุบันเป็นค่าเดียวกันทั้ง 12 เดือน ตอนนี้แต่ละเดือนเทียบกับเครื่องที่ต้องบันทึกยอดของเดือนนั้นจริง (#79)
- ระหว่างที่ยังตรวจยืนยันการติดตั้งไม่ครบ แดชบอร์ดบอกว่า "ยังยืนยันไม่ได้" พร้อมจำนวนเครื่องที่เหลือ แทนการประกาศว่าครบหรือค้าง ซึ่งทั้งสองคำเป็นข้อสรุปที่ข้อมูลยังไม่พอจะพูด (#79)
- ความครบถ้วนบนหน้าบันทึกยอดกับบนแดชบอร์ดเป็นตัวเลขเดียวกันแล้วเมื่อกรองอาคารหรือสัญญา เดิมหน้าบันทึกยอดนับทั้งระบบเสมอไม่ว่าจะกรองอะไรอยู่
- แถบเมนูไม่เปลี่ยนความกว้างเองเวลาเข้าและออกจากแดชบอร์ด (#51)
- คำว่า "ปีงบ" บนปุ่มเลือกปีงบประมาณที่แถบบนอ่านชัดขึ้น เดิมตัวอักษรจางลง 20% จน contrast เหลือ 4.1:1 ต่ำกว่าเกณฑ์ WCAG 1.4.3 (4.5:1) — ตรวจพบตอนเปลี่ยนแถบบนเป็นพื้นทึบ (#51)
- ค่าใช้จ่ายแท็บฝ่าย/แผนกคลิกกางฝ่าย แผนก และเครื่องได้อีกครั้ง เดิมกางได้เฉพาะปุ่ม "กางทั้งหมด" และเปิดยอดรายเดือนหรือลิงก์รายละเอียดของเครื่องจากแท็บนี้ไม่ได้ (#50)
- หน้าค่าใช้จ่ายแจ้งพร้อมปุ่มลองใหม่เมื่อโหลดรายการเครื่องที่ยังไม่ผูกสัญญาหรือรายการเดือนไม่สำเร็จ แทนการซ่อนแถบเตือนเงียบๆ จนดูเหมือนทุกเครื่องถูกนับในยอดแล้ว (#50)
- ช่องเลือกที่มีค่าเดิมเปิดแล้วแสดงตัวเลือกครบ ไม่ใช้ ID ที่เลือกเป็นคำค้นจนชื่ออาคารและรายการอื่นหาย (#48)
- ปุ่ม "ดูประวัติ" ในรายงานสรุปมีพื้นที่กดสูงอย่างน้อย 24px ตามเกณฑ์ 2.5.8 เดิมสูง 15px และเห็นเฉพาะแถวที่มีประวัติย้าย (#48)
- ชื่อแผนกในการ์ดเครื่องที่ใช้งานมากที่สุดขึ้นบรรทัดใหม่แทนการถูกตัดทิ้ง เมื่อผู้ใช้เพิ่มระยะห่างตัวอักษรตามเกณฑ์ 1.4.12 (#48)
- Dashboard แยกความครบถ้วนทั้งปี เช่น 6/12 เดือน ออกจากงานค้างเฉพาะเดือนที่จบแล้ว และเดือนที่ยังไม่ถึงกำหนด
- หน้าเปรียบเทียบรายเดือนเริ่มแสดงทุกเดือนที่มีข้อมูลตามตัวกรอง กราฟรายปีคลิกเลือกเดือนได้โดยยังเห็นบริบททั้งปี
- รายงานย้อนหลังกรองและจัดกลุ่มตามอาคาร ฝ่าย และแผนกที่เครื่องสังกัดในเดือนนั้น พร้อมกันยอดซ้ำเมื่อข้อมูลประวัติมีช่วงซ้อน
- โหมดมืดที่เพี้ยนเป็นหย่อมๆ ในบางหน้า — การ์ดที่ใช้สีเขียว/เหลือง/ม่วงไม่ถูกพลิกตามโหมดเหมือนสีเทา/ฟ้า/แดง ทำให้เป็นแผ่นสีจ้าโผล่กลางหน้าจอโทนมืด
- หัวตารางที่กดเพื่อเรียงลำดับได้ ใช้คีย์บอร์ดกดไม่ได้ (เป็น `<th @click>` ไม่ใช่ปุ่ม) และโปรแกรมอ่านหน้าจอไม่ประกาศว่ากำลังเรียงตามคอลัมน์ไหน
- หน้าต่างซ้อนและช่องเลือกที่เขียนเอง ไม่ขังโฟกัสไว้ข้างใน ไม่คืนโฟกัสตอนปิด และเลื่อนเลือกด้วยลูกศรไม่ได้
- ตัวเลขค่าใช้จ่ายบางรายการแสดงเศษทศนิยมเกินสองตำแหน่ง (เช่น 1209.6000000000001) และยอดรวมคลาดจากการคำนวณมือทีละสตางค์ — ตอนนี้คำนวณเป็นจำนวนเต็มสตางค์ทุกขั้น
- หน้าค่าใช้จ่ายและยอดพิมพ์รายเดือนแสดงข้อมูลว่างเปล่า ทั้งที่มีข้อมูลอยู่ในระบบ สาเหตุคือเดือนในฐานข้อมูลถูกบันทึกเป็น พ.ศ. ขณะที่ระบบกรองด้วย ค.ศ. ตอนนี้ระบบรับเดือนได้ทั้งสองแบบและแปลงให้อัตโนมัติ ดู [ADR-0002](docs/decisions/0002-store-months-in-common-era.md)
- ยอดพิมพ์และค่าใช้จ่ายรายเดือนไม่ตรงกับปีงบที่เลือก เพราะระบบเดาว่าปีงบตรงกับปีปฏิทิน (ม.ค.–ธ.ค.) แทนที่จะเป็นปีงบราชการไทยจริง (ต.ค.–ก.ย.) ดู [ADR-0001](docs/decisions/0001-thai-fiscal-year-oct-sep.md)
- ไฟล์ข้อมูลตัวอย่างสำหรับติดตั้งฐานข้อมูลใหม่รันไม่ผ่านมาตลอด เพราะมีเครื่องหมาย merge conflict ค้างอยู่ในไฟล์

### Security

- ฐานข้อมูลที่สร้างใหม่จาก `schema.sql` ไม่มีบัญชีที่ล็อกอินได้อีก มีเฉพาะบัญชี `admin` ที่ล็อกไว้จนกว่าจะตั้งรหัสด้วย `npm run db:bootstrap` (หรือตามคู่มือตั้งระบบ) — ฐานที่สร้างก่อนหน้านี้แต่ยังไม่เคยตั้งรหัสควรรัน `db:bootstrap` (#138)
- ปฏิเสธแถวในไฟล์นำเข้าทะเบียนอุปกรณ์เมื่อเลขซีเรียล ชื่อรุ่น หรือตำแหน่ง ขึ้นต้นด้วยเครื่องหมายสูตรคำนวณ (=, +, -, @) ยกเว้นเครื่องหมายขีดกลางหรือข้อความตัวแทนไม่มีข้อมูล (เช่น -ไม่มี-) และคุ้มกันการส่งออก CSV ไม่ให้รันสูตรแม้มีช่องว่างหรืออักขระล่องหนนำหน้า เพื่อความปลอดภัยเมื่อเปิดไฟล์ใน Excel (#134)
- ข้อมูลการเข้าสู่ระบบไม่ถูกเก็บไว้ในที่ที่สคริปต์ในหน้าเว็บอ่านได้อีกต่อไป และกดออกจากระบบแล้วหมดผลทันที
- อัปเดตไลบรารีอ่านไฟล์ Excel เป็นรุ่นล่าสุดจากผู้พัฒนาโดยตรง รุ่นบน npm ค้างอยู่ที่ตัวที่มีช่องโหว่และจะไม่ได้รับการแก้ไขอีก ดู [ADR-0003](docs/decisions/0003-sheetjs-from-vendor-registry.md)
- นำรหัสผ่านบัญชีผู้ดูแลระบบออกจากเอกสารและคอมเมนต์ใน schema
- API ไม่เปิดถ้า `JWT_SECRET` ไม่ได้ตั้ง สั้นกว่า 32 ตัวอักษร หรือยังเป็นค่าตัวอย่างใน `.env.example` — เดิมเปิดได้แล้วล็อกอินพัง 500 หรือใครก็ปลอม token ได้ เครื่องที่ตั้งค่าสั้นไว้ต้องตั้งใหม่ก่อนอัปเดต (#139)

## หมายเหตุ

โครงการนี้ยังไม่ได้ออกเวอร์ชันอย่างเป็นทางการ เมื่อเริ่มออกรุ่นให้ใช้ [Semantic Versioning](https://semver.org/lang/th/) และย้ายรายการจาก `Unreleased` ลงหัวข้อเวอร์ชันพร้อมวันที่
