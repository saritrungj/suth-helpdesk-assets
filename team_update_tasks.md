# 📌 สรุปอัปเดตงานและสิ่งที่ต้องแก้ไข (สำหรับส่งต่อให้ทีม)

## 1. 🛠️ การรื้อและปรับโครงสร้าง Database Schema ใหม่ (Database Normalization) 
เพื่อให้รองรับฟีเจอร์ Compare ข้อมูลข้ามมิติ (ปีงบ / ฝ่าย / แผนก / เครื่อง) ตาม Requirement ล่าสุด ต้องปรับ SQL Schema ดังนี้:

- **สร้างตาราง Master Data เพิ่มเติม (Lookup Tables):** ต้องแยกตาราง `brand`, `building`, `floor`, `division`, `department` และ `fiscal_year` ออกมาให้มี ID ของตัวเอง
- **อัปเดตตารางหลัก:**
  - เปลี่ยนชื่อตาราง `printers` เป็น `devices`
  - นำ ID จาก Master Data ด้านบนมาผูกเป็น Foreign Key ในตาราง `devices` ทั้งหมด
  - ส่วนตาราง `contracts` ให้ดึงข้อมูลปีงบประมาณออกมาเป็น `fiscal_year_id`

## 2. 📊 การปรับแก้ SQL Views สำหรับ Dashboard (งานฝั่ง Backend/DB)

- **แก้ Views เดิมที่พัง:** เนื่องจากมีการเปลี่ยนชื่อตารางและแยกตาราง (เช่น จาก `printers` เป็น `devices`) จึงต้องไล่แก้ JOIN ใน Views เดิมทั้งหมด (`v_monthly_kpi`, `v_summary_by_building` ฯลฯ)
- **สร้าง View ใหม่:** สำหรับฟีเจอร์ Compare ให้สร้าง View รวม (เช่น `v_compare_usage_costs`) ที่ดึงยอดการพิมพ์สุทธิ (80%) และราคาต่อแผ่น มาเพื่อให้ Frontend ใช้ดึงไปทำฟิลเตอร์ได้ง่ายๆ

## 3. ⚠️ ปัญหา Data Cleansing (รบกวนพี่ตะวันช่วย Confirm ข้อมูล) 
ก่อนที่ Backend จะนำข้อมูลดิบเข้า Database ใหม่ พบข้อมูลขัดแย้งในไฟล์ที่ต้องเคลียร์ก่อน:

- **ราคาต่อแผ่นซ้ำซ้อน:** สัญญา ส.205-2565 มีข้อมูลหลุดมา 2 ราคา คือ 0.34 และ 4.17 (ตกลงใช้ค่าไหน?)
- **ไม่มีราคา:** สัญญา SUTH 86-2567 ราคาปัจจุบันเป็น 0.0
- **ฝ่าย (Division) ไม่มีข้อมูล:** ข้อมูลดิบทั้งหมดไม่มีการระบุ "ฝ่าย" เบื้องต้นจะให้ DB ตั้งค่าเริ่มต้นเป็น "ยังไม่ระบุฝ่าย" ไปก่อนจนกว่าจะมีการอัปเดตข้อมูลจริง
- **ชื่ออาคารไม่สม่ำเสมอ:** ตัวสะกดตึกผิดเพี้ยน เช่น "สุรนิเวศ 17" กับ "สุรนิเวศ17" ต้อง Clean ข้อมูลให้ชื่อตรงกันก่อนนำเข้าตาราง `building`

## 4. 🐍 ปัญหา Script ดึงข้อมูล (Note ไว้สำหรับอนาคต)

- ไฟล์ `sql_extract.py` ปัจจุบันถูกเขียนมาเพื่อต่อกับ SQL Server แต่ Database โปรเจกต์เราคือ MySQL 8.4 (ถึงแม้ตอนนี้โอมจะพักการใช้ Python ไว้ก่อน แต่โน้ตไว้เผื่อมีการนำกลับมาใช้ จะได้แก้ Library ให้ถูกตัวครับ)
